---
quick_id: "260420-h12"
slug: "playwright-testing-requirement"
description: "Add Playwright as the mandatory E2E test framework to requirements and project docs"
date: "2026-04-20"
must_haves:
  truths:
    - "REQUIREMENTS.md contains TEST-01 requirement mandating Playwright for E2E tests"
    - "codebase/TESTING.md reflects actual test stack (Jest unit + Playwright E2E)"
    - "PROJECT.md constraints section includes Playwright as the E2E testing constraint"
  artifacts:
    - ".planning/REQUIREMENTS.md"
    - ".planning/codebase/TESTING.md"
    - ".planning/PROJECT.md"
---

# Quick Task 260420-h12: Add Playwright E2E Testing Requirement

## Task

Add Playwright as the mandatory E2E testing framework to all relevant planning documents.

## Plan

### Task 1 — Add TEST requirements to REQUIREMENTS.md

**File:** `.planning/REQUIREMENTS.md`

**Action:** Add a new `### Testing` subsection under `### Operations and Quality` with:
- `TEST-01`: E2E browser test suite uses Playwright as the sole browser automation framework for all UAT and integration tests.
- Add `TEST-01` to the Traceability table mapped to Phase 6.1 (already implemented).
- Update coverage count from 57 to 58.

**Verify:** `TEST-01` appears in REQUIREMENTS.md under Operations and Quality.

**Done:** TEST-01 requirement present in REQUIREMENTS.md.

---

### Task 2 — Update codebase/TESTING.md

**File:** `.planning/codebase/TESTING.md`

**Action:** Replace stale "Not detected" content with the actual test stack:
- Test runner: Jest (unit/integration for API), Playwright (E2E browser tests)
- Jest config: `apps/api/jest.config.ts`
- Playwright config: `playwright.config.ts` (root)
- Run commands: `pnpm test` (unit), `pnpm test:e2e` (E2E)
- E2E tests location: `e2e/` at repo root
- Global setup: `e2e/global-setup.ts` (creates fixtures, saves storageState)
- Test helpers: `e2e/helpers/api-setup.ts`, `e2e/helpers/ui-helpers.ts`
- Explicitly note: All browser-level and WebSocket UAT uses Playwright. No Cypress, no Selenium.

**Verify:** TESTING.md no longer says "Not detected" and mentions Playwright.

**Done:** TESTING.md reflects actual test stack.

---

### Task 3 — Add constraint to PROJECT.md

**File:** `.planning/PROJECT.md`

**Action:** Add to the `## Constraints` section:
- `**E2E Testing**: Playwright is the mandatory browser automation framework for all E2E and UAT tests — Cypress, Selenium, and other browser automation tools are out of scope.`

**Verify:** PROJECT.md Constraints section contains Playwright constraint.

**Done:** Playwright constraint present in PROJECT.md.
