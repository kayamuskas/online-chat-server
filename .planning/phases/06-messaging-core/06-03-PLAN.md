---
phase: 06-messaging-core
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - apps/api/src/messages/messages.controller.ts
  - apps/api/src/messages/messages.module.ts
  - apps/api/src/ws/app.gateway.ts
  - apps/api/src/app.module.ts
  - apps/api/src/__tests__/messages/messages-transport.spec.ts
autonomous: true
requirements:
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04
  - MSG-08
---

<objective>
Expose the message engine through authenticated HTTP and realtime transport so connected room and DM participants receive new and edited messages promptly.
</objective>

<context>
@.planning/phases/06-messaging-core/06-CONTEXT.md
@.planning/phases/06-messaging-core/06-RESEARCH.md
@apps/api/src/ws/app.gateway.ts
@apps/api/src/app.module.ts
@apps/api/src/contacts/contacts.controller.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add message HTTP endpoints and module wiring</name>
  <files>apps/api/src/messages/messages.controller.ts, apps/api/src/messages/messages.module.ts, apps/api/src/app.module.ts</files>
  <action>Create authenticated endpoints for room history/send/edit and DM history/send/edit, keeping route shapes parallel where possible. Wire the new messages module into the application with the same guard and controller patterns already used by rooms and contacts.</action>
</task>

<task type="auto">
  <name>Task 2: Add realtime message fanout and transport tests</name>
  <files>apps/api/src/ws/app.gateway.ts, apps/api/src/__tests__/messages/messages-transport.spec.ts</files>
  <action>Extend the authenticated socket transport to emit message-created and message-edited events to the appropriate room or DM participants. Keep history recovery on REST, but ensure connected clients receive push updates after successful writes. Add focused transport tests for DTO shape, authorization, and fanout behavior.</action>
  <verify>
    <automated>pnpm --filter @chat/api exec vitest run src/__tests__/messages/messages-transport.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - REST endpoints exist for room and DM history/send/edit
    - Realtime push reuses authenticated socket context rather than introducing a second auth flow
    - Transport tests cover both authorized and rejected messaging paths
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `pnpm --filter @chat/api exec vitest run src/__tests__/messages/messages-transport.spec.ts`
- [ ] `pnpm --filter @chat/api build`
</verification>

<output>
After completion, create `.planning/phases/06-messaging-core/06-03-SUMMARY.md`
</output>
