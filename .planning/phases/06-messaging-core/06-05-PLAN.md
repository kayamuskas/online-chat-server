---
phase: 06-messaging-core
plan: "05"
type: execute
wave: 5
depends_on:
  - "06-04"
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/features/messages/RoomChatView.tsx
  - apps/web/src/features/messages/DmChatView.tsx
  - apps/web/src/features/rooms/PublicRoomsView.tsx
  - apps/web/src/features/rooms/PrivateRoomsView.tsx
autonomous: false
requirements:
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04
  - MSG-08
---

<objective>
Integrate the new messaging engine into the authenticated shell so rooms and DMs use real chat views instead of stubs.
</objective>

<context>
@.planning/phases/06-messaging-core/06-CONTEXT.md
@.planning/phases/06-messaging-core/06-RESEARCH.md
@.planning/phases/05-contacts-and-dm-policy/05-05-SUMMARY.md
@apps/web/src/App.tsx
@apps/web/src/features/contacts/DmScreenStub.tsx
@apps/web/src/features/rooms/PublicRoomsView.tsx
@apps/web/src/features/rooms/PrivateRoomsView.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace DM stub with a real DM chat view</name>
  <files>apps/web/src/features/messages/DmChatView.tsx, apps/web/src/App.tsx</files>
  <action>Replace the Phase 5 DM stub with a real DM conversation view built on the shared messaging primitives. Preserve the existing access-denied and frozen-state semantics, but allow actual send/reply/edit flows when the conversation is eligible.</action>
</task>

<task type="auto">
  <name>Task 2: Add room-chat navigation and wire shell state</name>
  <files>apps/web/src/features/messages/RoomChatView.tsx, apps/web/src/App.tsx, apps/web/src/features/rooms/PublicRoomsView.tsx, apps/web/src/features/rooms/PrivateRoomsView.tsx</files>
  <action>Introduce an active room-chat surface in the authenticated shell. Public and private room entry points should open the selected room conversation after join or explicit open action. Reuse the shared messaging components so room chat and DM chat feel like the same engine from the user's perspective.</action>
  <verify>
    <automated>pnpm --filter @chat/web build</automated>
  </verify>
  <acceptance_criteria>
    - DM tab no longer shows the Phase 5 placeholder when the conversation is eligible
    - Joined rooms can be opened as real chat conversations from the existing shell
    - Reply, edit marker, chronological render, and watermark metadata all surface in the integrated UI
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real messaging flows in the browser</name>
  <what_built>The shell now exposes real room and DM conversations backed by the shared Phase 6 message engine.</what_built>
  <how_to_verify>
    1. Open a room chat and send a multiline message.
    2. Reply to that message and confirm the reply reference is visible.
    3. Edit your own message and confirm the edited marker appears.
    4. Open an eligible DM conversation and confirm the same core send/reply/edit behaviors exist there too.
    5. Reload the page and confirm recent history comes back in chronological order.
  </how_to_verify>
  <resume_signal>Type `approved` if the browser flow matches, or describe the first mismatch.</resume_signal>
</task>
</tasks>

<verification>
- [ ] `pnpm --filter @chat/web build`
- [ ] Human browser verification for room and DM messaging
</verification>

<output>
After completion, create `.planning/phases/06-messaging-core/06-05-SUMMARY.md`
</output>
