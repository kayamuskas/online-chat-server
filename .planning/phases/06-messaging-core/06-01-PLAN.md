---
phase: 06-messaging-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/messages/messages.types.ts
  - apps/api/src/db/migrations/0005_messages_core.sql
  - apps/api/src/__tests__/messages/messages-domain.spec.ts
  - apps/api/src/__tests__/messages/messages-watermarks.spec.ts
autonomous: true
requirements:
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04
  - MSG-08
---

<objective>
Create the durable message schema, type contracts, and test scaffold for the shared room/DM message engine.
</objective>

<context>
@.planning/phases/06-messaging-core/06-CONTEXT.md
@.planning/phases/06-messaging-core/06-RESEARCH.md
@.planning/phases/05-contacts-and-dm-policy/05-02-SUMMARY.md
@apps/api/src/contacts/contacts.types.ts
@apps/api/src/db/postgres.service.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define shared message types and migration</name>
  <files>apps/api/src/messages/messages.types.ts, apps/api/src/db/migrations/0005_messages_core.sql</files>
  <action>Create the shared message domain types and migration for a single message table that supports room and DM conversations, reply references, edit metadata, and per-conversation watermarks. Reuse existing naming and migration conventions from rooms and contacts. The schema must support author-only edits later without needing destructive rewrites.</action>
  <acceptance_criteria>
    - Message type contracts cover room and DM targets, reply metadata, edited state, and watermark fields
    - Migration creates the messages table and any required indexes/constraints for conversation-scoped ordering
    - No attachment columns or deletion-state columns are introduced in Phase 6
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED/GREEN test scaffold for messaging invariants</name>
  <files>apps/api/src/__tests__/messages/messages-domain.spec.ts, apps/api/src/__tests__/messages/messages-watermarks.spec.ts</files>
  <action>Seed focused tests for shared messaging behavior: send, reply validation, edit metadata, chronological history, and watermark monotonicity. Keep the scope phase-local and avoid attachment or deletion behavior that belongs to later phases.</action>
  <verify>
    <automated>pnpm --filter @chat/api exec vitest run src/__tests__/messages/messages-domain.spec.ts src/__tests__/messages/messages-watermarks.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests name the MSG-01/02/03/04/08 invariants explicitly
    - Watermark tests fail loudly if per-conversation sequencing breaks
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `pnpm --filter @chat/api exec vitest run src/__tests__/messages/messages-domain.spec.ts src/__tests__/messages/messages-watermarks.spec.ts`
</verification>

<output>
After completion, create `.planning/phases/06-messaging-core/06-01-SUMMARY.md`
</output>
