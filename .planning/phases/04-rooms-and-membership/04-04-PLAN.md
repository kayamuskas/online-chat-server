---
phase: 04-rooms-and-membership
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/styles.css
  - apps/web/src/features/rooms/PublicRoomsView.tsx
  - apps/web/src/features/rooms/CreateRoomView.tsx
  - apps/web/src/features/rooms/PrivateRoomsView.tsx
  - apps/web/src/features/rooms/ManageRoomView.tsx
  - apps/web/src/features/rooms/RoomMembersTable.tsx
  - apps/web/src/features/rooms/RoomBanListView.tsx
  - apps/api/src/__tests__/rooms/rooms-web-contract.spec.ts
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-04
  - ROOM-05
  - ROOM-06
  - ROOM-07
  - ROOM-08
  - ROOM-10
  - ROOM-11
must_haves:
  truths:
    - "The web app exposes a real Phase 4 room surface for public-room discovery, create-room, private invites, and room management."
    - "Room-management UI exposes owner/admin/member and ban-list actions in the classic chat-shell direction already approved."
    - "Owner leave refusal, invite-by-username, and public catalog search are visible end-user behaviors rather than backend-only capabilities."
  artifacts:
    - path: "apps/web/src/features/rooms/PublicRoomsView.tsx"
      provides: "Public-room catalog/search UI"
    - path: "apps/web/src/features/rooms/ManageRoomView.tsx"
      provides: "Room-management UI for admins/owner"
    - path: "apps/web/src/lib/api.ts"
      provides: "Client bindings for Phase 4 room surfaces"
  key_links:
    - from: "apps/web/src/features/rooms/PublicRoomsView.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "catalog/search/join/create room client calls"
      pattern: "list rooms|search|join|create"
    - from: "apps/web/src/features/rooms/ManageRoomView.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "invite/admin/ban management client calls"
      pattern: "invite|admin|ban|unban|remove"
    - from: "apps/web/src/features/rooms/RoomMembersTable.tsx"
      to: "apps/web/src/features/presence"
      via: "member-table status rendering"
      pattern: "PresenceDot|PresenceLabel|PresenceTimestamp"
---

<objective>
Ship the Phase 4 room UI on top of the new room and management APIs.

Purpose: make room creation, public/private room discovery, membership transitions, and room management user-visible in the classic chat shell.
Output: room-focused web views, API client bindings, and integrated room-management UI.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rooms-and-membership/04-CONTEXT.md
@.planning/phases/04-rooms-and-membership/04-RESEARCH.md
@requirements/wireframes.md
@requirements/desing_v1/components/rooms.jsx
@requirements/desing_v1/components/manage.jsx
@apps/web/src/App.tsx
@apps/web/src/lib/api.ts
@apps/web/src/styles.css
@apps/web/src/features/presence

<interfaces>
Phase 4 room UI must expose:

- public-room catalog with search by name + description
- create-room flow with `name` required, `public` default, optional description
- private-room invite flow by username and from available UI lists
- immediate member leave and explicit owner leave refusal
- owner/admin/member management UI and ban-list operations

Do not expand into room messaging, unread indicators, or attachment UI here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public/private room surfaces and create-room UI</name>
  <read_first>.planning/phases/04-rooms-and-membership/04-CONTEXT.md, .planning/phases/04-rooms-and-membership/04-RESEARCH.md, requirements/wireframes.md, requirements/desing_v1/components/rooms.jsx, apps/web/src/App.tsx, apps/web/src/lib/api.ts</read_first>
  <files>apps/web/src/App.tsx, apps/web/src/lib/api.ts, apps/web/src/styles.css, apps/web/src/features/rooms/PublicRoomsView.tsx, apps/web/src/features/rooms/CreateRoomView.tsx, apps/web/src/features/rooms/PrivateRoomsView.tsx</files>
  <action>Extend the existing authenticated shell with explicit room surfaces instead of hiding rooms behind placeholder tabs. Implement a public-room catalog that shows `name`, `description`, and `member count`, with search over name + description. Add the lightweight create-room flow that requires only `name`, defaults visibility to `public`, and keeps description optional. Add the private-room surface that can show invite entry points and room-specific private-room state without leaking private rooms into the public catalog.</action>
  <verify>rg -n "Create room|Public rooms|Private rooms|member count|search" apps/web/src/features/rooms apps/web/src/App.tsx apps/web/src/lib/api.ts && pnpm --filter @chat/web build</verify>
  <acceptance_criteria>
    - Public-room UI visibly shows `name`, `description`, and `member count`.
    - Create-room UI preserves the locked required/default field contract.
    - Private-room UI remains distinct from the public catalog.
  </acceptance_criteria>
  <done>Users can discover rooms and create new ones through real web surfaces aligned with the approved chat-shell direction.</done>
</task>

<task type="auto">
  <name>Task 2: Add room-management UI for member, admin, and ban-list operations</name>
  <read_first>.planning/phases/04-rooms-and-membership/04-CONTEXT.md, .planning/phases/04-rooms-and-membership/04-RESEARCH.md, requirements/wireframes.md, requirements/desing_v1/components/manage.jsx, apps/web/src/features/presence</read_first>
  <files>apps/web/src/lib/api.ts, apps/web/src/styles.css, apps/web/src/features/rooms/ManageRoomView.tsx, apps/web/src/features/rooms/RoomMembersTable.tsx, apps/web/src/features/rooms/RoomBanListView.tsx, apps/web/src/features/rooms/PrivateRoomsView.tsx</files>
  <action>Build the room-management UI that the locked context explicitly pulled into Phase 4. The UI must expose owner/admin/member management actions, ban-list operations, and invite-by-username. Owner leave refusal must be surfaced clearly to the user, not hidden as a generic failure. Reuse the shared presence primitives for member-status rendering instead of inventing one-off indicators. Preserve the classic chat-shell and modal/panel direction from the reference materials rather than introducing a detached admin dashboard.</action>
  <verify>rg -n "Manage room|Make admin|Remove admin|Ban|Unban|Invite by username|cannot leave" apps/web/src/features/rooms apps/web/src/styles.css && pnpm --filter @chat/web build</verify>
  <acceptance_criteria>
    - Room-management UI exposes owner/admin/member and ban-list operations.
    - Invite-by-username is visible in the management UI.
    - Owner leave attempts surface a clear refusal message.
  </acceptance_criteria>
  <done>Phase 4 room-management behavior is visible and operable in the web app, not just hidden behind backend endpoints.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @chat/web build`
- [ ] `rg -n "Public rooms|Create room|Private rooms|Manage room|Ban|Unban|Invite by username" apps/web/src/features/rooms apps/web/src/App.tsx`
- [ ] The room UI stays within the classic chat shell rather than becoming a separate dashboard flow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Room creation, discovery, invite, membership, and management flows are user-visible in the web app
  </success_criteria>

<output>
After completion, create `.planning/phases/04-rooms-and-membership/04-04-SUMMARY.md`
</output>
