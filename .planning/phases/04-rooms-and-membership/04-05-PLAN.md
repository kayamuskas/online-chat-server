---
phase: 04-rooms-and-membership
plan: 05
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - apps/api/src/rooms/rooms.repository.ts
  - apps/api/src/rooms/rooms.service.ts
  - apps/api/src/rooms/rooms.controller.ts
  - apps/api/src/rooms/rooms-management.controller.ts
  - apps/api/src/rooms/rooms.module.ts
  - apps/api/src/__tests__/rooms/rooms-private-membership.spec.ts
  - apps/web/src/lib/api.ts
  - apps/web/src/App.tsx
  - apps/web/src/features/rooms/PrivateRoomsView.tsx
autonomous: true
gap_closure: true
requirements:
  - ROOM-04
  - ROOM-06
  - ROOM-11
must_haves:
  truths:
    - "An invited user can see pending private-room invites and explicitly accept or decline them."
    - "Accepting a valid private-room invite turns the invite into membership and makes the room visible in the current user's private-room surface."
    - "The web app loads the authenticated user's private-room memberships from the API instead of hardcoding an empty list."
  artifacts:
    - path: "apps/api/src/rooms/rooms.service.ts"
      provides: "Invite-recipient actions and authenticated private-room membership query methods"
    - path: "apps/api/src/rooms/rooms.controller.ts"
      provides: "Recipient-facing HTTP endpoints for private invite actions and private-room listing"
    - path: "apps/web/src/App.tsx"
      provides: "Real private-room and pending-invite data flow into PrivateRoomsView"
    - path: "apps/web/src/features/rooms/PrivateRoomsView.tsx"
      provides: "UI for pending invites plus populated private-room memberships"
  key_links:
    - from: "apps/api/src/rooms/rooms.controller.ts"
      to: "apps/api/src/rooms/rooms.service.ts"
      via: "GET /api/v1/rooms/mine/private and POST /api/v1/rooms/:id/invites/:inviteId/(accept|decline)"
      pattern: "mine/private|invites/.*/accept|invites/.*/decline"
    - from: "apps/web/src/lib/api.ts"
      to: "apps/api/src/rooms/rooms.controller.ts"
      via: "getMyPrivateRooms, getPendingPrivateInvites, acceptRoomInvite, declineRoomInvite"
      pattern: "getMyPrivateRooms|getPendingPrivateInvites|acceptRoomInvite|declineRoomInvite"
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/features/rooms/PrivateRoomsView.tsx"
      via: "loaded rooms and invites props plus accept/decline handlers"
      pattern: "PrivateRoomsView|privateRooms|pendingInvites"
---

<objective>
Close the two unresolved Phase 4 verification gaps around private-room lifecycle completion.

Purpose: finish ROOM-04 end-to-end by giving invited users a real accept/decline path and by wiring authenticated private-room membership data into the web shell.
Output: recipient-facing invite endpoints, authenticated private-room listing, focused backend coverage, and real App/PrivateRoomsView data flow.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rooms-and-membership/04-CONTEXT.md
@.planning/phases/04-rooms-and-membership/04-RESEARCH.md
@.planning/phases/04-rooms-and-membership/04-VERIFICATION.md
@.planning/phases/04-rooms-and-membership/04-04-SUMMARY.md
@.planning/phases/04-rooms-and-membership/04-04-PLAN.md
@apps/api/src/rooms/rooms.repository.ts
@apps/api/src/rooms/rooms.service.ts
@apps/api/src/rooms/rooms.controller.ts
@apps/api/src/rooms/rooms-management.controller.ts
@apps/web/src/lib/api.ts
@apps/web/src/App.tsx
@apps/web/src/features/rooms/PrivateRoomsView.tsx

<interfaces>
Gap-closure contracts to add:

- `RoomsRepository.listPrivateRoomsByUser(userId)` returning the authenticated user's private-room memberships with room details and member counts.
- `RoomsRepository.listPendingInvitesByUser(userId)` and `RoomsRepository.findInviteForRecipient(roomId, inviteId, userId)` so accept/decline actions validate invite ownership.
- `RoomsService.getMyPrivateRooms(userId)`, `RoomsService.getPendingPrivateInvites(userId)`, `RoomsService.acceptInvite(roomId, inviteId, userId)`, and `RoomsService.declineInvite(roomId, inviteId, userId)`.
- `GET /api/v1/rooms/mine/private`
- `GET /api/v1/rooms/invites/pending`
- `POST /api/v1/rooms/:id/invites/:inviteId/accept`
- `POST /api/v1/rooms/:id/invites/:inviteId/decline`
- Web client functions: `getMyPrivateRooms()`, `getPendingPrivateInvites()`, `acceptRoomInvite(roomId, inviteId)`, `declineRoomInvite(roomId, inviteId)`.

Keep recipient-facing invite actions out of `rooms-management.controller.ts`; that controller is for owner/admin actions, while these new endpoints are for the invited authenticated user.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recipient invite actions and authenticated private-room queries to the backend</name>
  <read_first>.planning/phases/04-rooms-and-membership/04-VERIFICATION.md, apps/api/src/rooms/rooms.repository.ts, apps/api/src/rooms/rooms.service.ts, apps/api/src/rooms/rooms.controller.ts, apps/api/src/rooms/rooms-management.controller.ts</read_first>
  <files>apps/api/src/rooms/rooms.repository.ts, apps/api/src/rooms/rooms.service.ts, apps/api/src/rooms/rooms.controller.ts, apps/api/src/rooms/rooms-management.controller.ts, apps/api/src/rooms/rooms.module.ts, apps/api/src/__tests__/rooms/rooms-private-membership.spec.ts</files>
  <behavior>
    - Test 1: `GET /api/v1/rooms/mine/private` returns only private rooms where the authenticated user has active membership, with the membership role and current member count.
    - Test 2: `GET /api/v1/rooms/invites/pending` returns only pending invites addressed to the authenticated user.
    - Test 3: `POST /api/v1/rooms/:id/invites/:inviteId/accept` rejects invites that belong to another user or another room, accepts only pending invites, adds membership once, and updates invite status to `accepted`.
    - Test 4: `POST /api/v1/rooms/:id/invites/:inviteId/decline` rejects non-owned/non-pending invites and updates a valid pending invite to `declined` without creating membership.
  </behavior>
  <action>Add the missing repository and service layer needed to close both verification gaps. In `rooms.repository.ts`, add `listPrivateRoomsByUser`, `listPendingInvitesByUser`, and `findInviteForRecipient` query methods instead of reusing the public catalog projection. Use the existing `acceptInvite` and `declineInvite` writes only through new service methods that validate invite ownership, room match, pending state, existing membership, and private-room visibility before mutating anything. In `rooms.service.ts`, implement `getMyPrivateRooms(userId)`, `getPendingPrivateInvites(userId)`, `acceptInvite(roomId, inviteId, userId)`, and `declineInvite(roomId, inviteId, userId)`; accepting an invite must add the user as `member` and mark the invite accepted in the same logical flow, while decline only updates invite status. In `rooms.controller.ts`, add exact authenticated recipient endpoints `GET /api/v1/rooms/mine/private`, `GET /api/v1/rooms/invites/pending`, `POST /api/v1/rooms/:id/invites/:inviteId/accept`, and `POST /api/v1/rooms/:id/invites/:inviteId/decline`. Keep `rooms-management.controller.ts` unchanged except for import/module wiring if needed; do not move admin-only endpoints there. Create `apps/api/src/__tests__/rooms/rooms-private-membership.spec.ts` covering listing, accept, decline, ownership rejection, and duplicate-accept protection.</action>
  <verify>
    <automated>pnpm --filter @chat/api test -- --run rooms-private-membership</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mine/private|invites/pending|invites/:inviteId/accept|invites/:inviteId/decline" apps/api/src/rooms/rooms.controller.ts` finds the four new authenticated routes.
    - `rg -n "getMyPrivateRooms|getPendingPrivateInvites|acceptInvite|declineInvite" apps/api/src/rooms/rooms.service.ts` finds the concrete service methods.
    - `rg -n "listPrivateRoomsByUser|listPendingInvitesByUser|findInviteForRecipient" apps/api/src/rooms/rooms.repository.ts` finds the new query methods.
    - `pnpm --filter @chat/api test -- --run rooms-private-membership` passes.
  </acceptance_criteria>
  <done>The backend exposes a complete private-room recipient flow: invited users can list pending invites, accept or decline only their own invites, and list their actual private-room memberships through authenticated endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the hollow private-room UI with live membership and invite data</name>
  <read_first>.planning/phases/04-rooms-and-membership/04-VERIFICATION.md, apps/web/src/lib/api.ts, apps/web/src/App.tsx, apps/web/src/features/rooms/PrivateRoomsView.tsx</read_first>
  <files>apps/web/src/lib/api.ts, apps/web/src/App.tsx, apps/web/src/features/rooms/PrivateRoomsView.tsx</files>
  <action>Wire the new backend surfaces into the existing Phase 4 shell instead of adding a second room dashboard. In `apps/web/src/lib/api.ts`, add the exact client bindings `getMyPrivateRooms`, `getPendingPrivateInvites`, `acceptRoomInvite`, and `declineRoomInvite` matching the new backend routes. In `apps/web/src/App.tsx`, replace `rooms={[]}` with real state: fetch the authenticated user's private rooms and pending invites after sign-in/session restore, refresh those datasets after invite acceptance or decline, and keep `ManageRoomView` navigation working against the loaded room rows. In `apps/web/src/features/rooms/PrivateRoomsView.tsx`, extend the view to render a pending-invites section above the room list, with explicit Accept and Decline buttons for each invite recipient entry, while preserving the existing private-room membership list and empty-state copy. Do not fetch inside `PrivateRoomsView`; keep App as the data owner consistent with the current shell pattern.</action>
  <verify>
    <automated>pnpm --filter @chat/web build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "getMyPrivateRooms|getPendingPrivateInvites|acceptRoomInvite|declineRoomInvite" apps/web/src/lib/api.ts` finds the four new client bindings.
    - `rg -n "rooms=\\{\\[\\]\\}" apps/web/src/App.tsx` returns no matches.
    - `rg -n "pending invite|Accept|Decline|Loading private rooms" apps/web/src/features/rooms/PrivateRoomsView.tsx` shows the invite-action UI in the private-room surface.
    - `pnpm --filter @chat/web build` passes.
  </acceptance_criteria>
  <done>The private-room surface is no longer hollow: the app loads real memberships, shows pending invites to the invited user, and lets the user accept or decline invites without leaving the Phase 4 shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> rooms recipient endpoints | Authenticated but user-controlled invite and room identifiers cross into the API |
| service -> repository | Invite acceptance and private-room listing rely on policy-filtered database queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | E | `POST /api/v1/rooms/:id/invites/:inviteId/accept` | mitigate | `RoomsService.acceptInvite` must validate authenticated recipient ownership, room match, and pending status before adding membership or updating invite state |
| T-04-05-02 | I | `GET /api/v1/rooms/mine/private` and `GET /api/v1/rooms/invites/pending` | mitigate | Repository queries must filter strictly by authenticated `userId` and private visibility so another user's memberships or invite metadata never leak |
| T-04-05-03 | T | invite acceptance flow | mitigate | Service logic must reject duplicate accept/decline on non-pending invites and avoid duplicate membership insertion when membership already exists |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @chat/api test -- --run rooms-private-membership`
- [ ] `pnpm --filter @chat/web build`
- [ ] `rg -n "mine/private|invites/pending|invites/:inviteId/accept|invites/:inviteId/decline" apps/api/src/rooms/rooms.controller.ts`
- [ ] `rg -n "getMyPrivateRooms|getPendingPrivateInvites|acceptRoomInvite|declineRoomInvite" apps/web/src/lib/api.ts`
- [ ] `rg -n "rooms=\\{\\[\\]\\}" apps/web/src/App.tsx` returns no matches
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No deferred verification gaps from `04-VERIFICATION.md` are replanned outside the two actionable `gaps:` items
- Invited users can act on private-room invites and the web shell shows the authenticated user's real private-room memberships
  </success_criteria>

<output>
After completion, create `.planning/phases/04-rooms-and-membership/04-05-SUMMARY.md`
</output>
