---
phase: 04-rooms-and-membership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/db/postgres.service.ts
  - apps/api/src/db/migrations/0003_rooms_core.sql
  - apps/api/src/rooms/rooms.types.ts
  - apps/api/src/rooms/rooms.repository.ts
  - apps/api/src/rooms/rooms.service.ts
  - apps/api/src/rooms/rooms.module.ts
  - apps/api/src/__tests__/rooms/rooms-domain.spec.ts
autonomous: true
requirements:
  - ROOM-01
  - ROOM-10
  - ROOM-11
must_haves:
  truths:
    - "Rooms become a first-class durable domain with globally unique names, explicit visibility, owner identity, and membership-related relations."
    - "Private-room invitations target only already registered users and are representable durably before UI work starts."
    - "Room authority state needed by later plans exists explicitly: owner, admins, members, invites, and room-level bans."
  artifacts:
    - path: "apps/api/src/db/migrations/0003_rooms_core.sql"
      provides: "PostgreSQL schema for rooms, room memberships, invites, admins, and bans"
    - path: "apps/api/src/rooms/rooms.repository.ts"
      provides: "Persistence boundary for room identity and authority relations"
    - path: "apps/api/src/rooms/rooms.service.ts"
      provides: "Core room domain rules for creation and authority primitives"
  key_links:
    - from: "apps/api/src/rooms/rooms.service.ts"
      to: "apps/api/src/rooms/rooms.repository.ts"
      via: "room creation and authority persistence"
      pattern: "create room|owner|admin|invite|ban"
    - from: "apps/api/src/rooms/rooms.service.ts"
      to: "apps/api/src/auth/user.repository.ts"
      via: "registered-user validation for invite targets"
      pattern: "username|user lookup|registered"
---

<objective>
Create the durable room-domain foundation for Phase 4.

Purpose: establish the PostgreSQL-backed model and service boundary that every later room, membership, messaging, and attachment rule will depend on.
Output: schema evolution, room domain types/repository/service/module, and focused backend tests for identity and authority foundations.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rooms-and-membership/04-CONTEXT.md
@.planning/phases/04-rooms-and-membership/04-RESEARCH.md
@requirements/requirements_raw.md
@requirements/desing_v1/components/rooms.jsx
@requirements/desing_v1/components/manage.jsx
@apps/api/src/db/postgres.service.ts
@apps/api/src/auth/user.repository.ts

<interfaces>
Phase 4 room-domain foundation must provide:

- globally unique room names across public and private rooms
- room visibility (`public` / `private`)
- explicit owner/admin/member/ban/invite relations
- invite validation against already registered users only
- durable room metadata suitable for later catalog and management APIs

Do not implement room messages, unread state, or attachment behavior here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 4 room-domain schema and typed repository boundary</name>
  <read_first>.planning/phases/04-rooms-and-membership/04-CONTEXT.md, .planning/phases/04-rooms-and-membership/04-RESEARCH.md, requirements/requirements_raw.md, apps/api/src/db/postgres.service.ts, apps/api/src/auth/user.repository.ts</read_first>
  <files>apps/api/src/db/postgres.service.ts, apps/api/src/db/migrations/0003_rooms_core.sql, apps/api/src/rooms/rooms.types.ts, apps/api/src/rooms/rooms.repository.ts, apps/api/src/__tests__/rooms/rooms-domain.spec.ts</files>
  <action>Introduce the durable Phase 4 schema for rooms and authority relations. Model rooms, memberships, invites, admins, and room bans explicitly in PostgreSQL rather than collapsing them into one table or JSON fields. Keep room-name uniqueness global across all visibilities. Ensure invite rows can only reference already registered users through normal user lookup and foreign-key-compatible IDs. Extend bootstrap SQL in `postgres.service.ts` the same way prior phases kept fresh-start compatibility with migrations.</action>
  <verify>rg -n "rooms|memberships|invites|bans|owner|admin|unique" apps/api/src/db/migrations apps/api/src/rooms && pnpm --filter @chat/api test -- --run rooms-domain</verify>
  <acceptance_criteria>
    - The new SQL migration creates durable relations for rooms, memberships, invites, admins, and room bans.
    - Room names are enforced as globally unique at the database level.
    - Room repository/types expose explicit owner/admin/member/ban/invite concepts rather than implicit booleans only.
  </acceptance_criteria>
  <done>Phase 4 has a durable room-domain foundation that later catalog, membership, and management APIs can build on without reworking schema ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add core room service rules for creation and authority primitives</name>
  <read_first>.planning/phases/04-rooms-and-membership/04-CONTEXT.md, .planning/phases/04-rooms-and-membership/04-RESEARCH.md, requirements/requirements_raw.md, apps/api/src/auth/user.repository.ts, apps/api/src/rooms/rooms.repository.ts</read_first>
  <files>apps/api/src/rooms/rooms.service.ts, apps/api/src/rooms/rooms.module.ts, apps/api/src/__tests__/rooms/rooms-domain.spec.ts</files>
  <action>Create the initial `RoomsService` that owns Phase 4 domain invariants before HTTP endpoints exist: room creation with `name` required, `visibility=public` default, optional description, owner bootstrap as the room creator, and registered-user validation helpers for invite targeting. Also expose the authority primitives that later plans will use for admin/member/ban behavior instead of embedding business rules in controllers.</action>
  <verify>rg -n "createRoom|public|private|owner|registered|invite" apps/api/src/rooms apps/api/src/__tests__/rooms/rooms-domain.spec.ts && pnpm --filter @chat/api test -- --run rooms-domain</verify>
  <acceptance_criteria>
    - Creating a room requires only `name`; `visibility` defaults to `public`; `description` remains optional.
    - The room creator is durably recorded as owner.
    - Service-level helpers exist for later invite/admin/ban/member flows.
  </acceptance_criteria>
  <done>The backend has a policy-first room domain service instead of postponing core room rules to controller code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @chat/api test -- --run rooms-domain`
- [ ] `rg -n "CREATE TABLE|ALTER TABLE|UNIQUE|invite|ban|admin" apps/api/src/db/migrations/0003_rooms_core.sql apps/api/src/rooms`
- [ ] Fresh-start bootstrap SQL stays aligned with the migration additions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Room identity and authority state are durable and explicit before API/UI expansion begins
  </success_criteria>

<output>
After completion, create `.planning/phases/04-rooms-and-membership/04-01-SUMMARY.md`
</output>
