---
phase: 04-rooms-and-membership
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/api/src/rooms/rooms.controller.ts
  - apps/api/src/rooms/rooms.service.ts
  - apps/api/src/rooms/rooms.repository.ts
  - apps/api/src/rooms/rooms.module.ts
  - apps/api/src/__tests__/rooms/rooms-catalog.spec.ts
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-04
  - ROOM-05
  - ROOM-06
must_haves:
  truths:
    - "Authenticated users can create rooms, browse/search the public catalog, join public rooms unless banned, and leave rooms unless they are the owner."
    - "Private rooms remain hidden from the public catalog and joinable only by invitation."
    - "Owner-leave attempts fail explicitly instead of silently mutating membership state."
  artifacts:
    - path: "apps/api/src/rooms/rooms.controller.ts"
      provides: "Authenticated HTTP surface for room creation, catalog, join, and leave flows"
    - path: "apps/api/src/__tests__/rooms/rooms-catalog.spec.ts"
      provides: "Focused tests for catalog visibility, joinability, and owner leave constraints"
  key_links:
    - from: "apps/api/src/rooms/rooms.controller.ts"
      to: "apps/api/src/rooms/rooms.service.ts"
      via: "create/catalog/join/leave API orchestration"
      pattern: "create|catalog|join|leave"
    - from: "apps/api/src/rooms/rooms.service.ts"
      to: "apps/api/src/rooms/rooms.repository.ts"
      via: "public/private visibility and membership policy enforcement"
      pattern: "public|private|banned|owner"
---

<objective>
Expose the first end-user room flows for creation, discovery, joining, and leaving.

Purpose: turn the new room domain into an authenticated API surface that already satisfies the public/private catalog and membership-lifecycle rules.
Output: room creation/catalog/join/leave endpoints and focused tests.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-rooms-and-membership/04-CONTEXT.md
@.planning/phases/04-rooms-and-membership/04-RESEARCH.md
@.planning/phases/04-rooms-and-membership/04-01-PLAN.md
@requirements/requirements_raw.md
@apps/api/src/auth/current-user.guard.ts
@apps/api/src/rooms/rooms.repository.ts
@apps/api/src/rooms/rooms.service.ts

<interfaces>
This plan must provide:

- create room endpoint with minimal room-creation contract
- public room catalog endpoint with search over name + description
- member-count projection in catalog responses
- join public room unless banned
- leave room for ordinary members
- explicit refusal when the owner attempts to leave

Do not add admin/ban-list management endpoints here; that belongs to the next plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add authenticated create-room and public-catalog API surface</name>
  <read_first>.planning/phases/04-rooms-and-membership/04-CONTEXT.md, .planning/phases/04-rooms-and-membership/04-RESEARCH.md, requirements/requirements_raw.md, apps/api/src/auth/current-user.guard.ts, apps/api/src/rooms/rooms.service.ts</read_first>
  <files>apps/api/src/rooms/rooms.controller.ts, apps/api/src/rooms/rooms.service.ts, apps/api/src/rooms/rooms.repository.ts, apps/api/src/rooms/rooms.module.ts, apps/api/src/__tests__/rooms/rooms-catalog.spec.ts</files>
  <action>Add authenticated REST endpoints for room creation and public-room catalog/search. The create-room endpoint must accept only the minimal locked contract: required `name`, optional `description`, optional `visibility` that defaults to `public`. The catalog endpoint must return name, description, member count, and enough identity data for later UI work, while ensuring private rooms never appear in the public projection. Search must match both room name and description.</action>
  <verify>rg -n "create room|catalog|search|member count|public|private" apps/api/src/rooms apps/api/src/__tests__/rooms/rooms-catalog.spec.ts && pnpm --filter @chat/api test -- --run rooms-catalog</verify>
  <acceptance_criteria>
    - Public catalog responses include `name`, `description`, and `member count`.
    - Catalog search matches both room name and description.
    - Private rooms are excluded from public catalog/search results.
  </acceptance_criteria>
  <done>Authenticated users can create rooms and browse/search the public catalog without leaking private room visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add join and leave room flows with owner restriction</name>
  <read_first>.planning/phases/04-rooms-and-membership/04-CONTEXT.md, requirements/requirements_raw.md, apps/api/src/rooms/rooms.service.ts, apps/api/src/rooms/rooms.repository.ts</read_first>
  <files>apps/api/src/rooms/rooms.controller.ts, apps/api/src/rooms/rooms.service.ts, apps/api/src/rooms/rooms.repository.ts, apps/api/src/__tests__/rooms/rooms-catalog.spec.ts</files>
  <action>Implement authenticated join and leave operations. Joining a public room must succeed for any authenticated user unless they are banned. Leaving must remove ordinary-member membership immediately with no confirmation semantics embedded in the backend; owner leave attempts must fail with a clear service/controller error that the UI can surface directly. Preserve the distinction between room membership and room ban state so later admin flows do not need to reinterpret these transitions.</action>
  <verify>rg -n "join|leave|owner|banned" apps/api/src/rooms apps/api/src/__tests__/rooms/rooms-catalog.spec.ts && pnpm --filter @chat/api test -- --run rooms-catalog join leave</verify>
  <acceptance_criteria>
    - Public-room join succeeds for authenticated non-banned users.
    - Owner leave attempts fail explicitly.
    - Ordinary-member leave removes membership cleanly without deleting the room.
  </acceptance_criteria>
  <done>Phase 4 exposes the core membership lifecycle the web UI will consume for catalog and room-entry flows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @chat/api test -- --run rooms-catalog`
- [ ] `rg -n "catalog|search|join|leave|owner|banned" apps/api/src/rooms`
- [ ] Private rooms remain absent from public catalog responses
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Core create/catalog/join/leave rules match the locked Phase 4 contract
  </success_criteria>

<output>
After completion, create `.planning/phases/04-rooms-and-membership/04-02-SUMMARY.md`
</output>
