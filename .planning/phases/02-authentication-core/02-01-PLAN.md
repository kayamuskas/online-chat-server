---
phase: 02-authentication-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - packages/shared/src/config.ts
  - apps/api/src/db/postgres.service.ts
  - apps/api/src/db/db.module.ts
  - apps/api/src/db/migrations/0001_auth_core.sql
  - apps/api/src/auth/auth.types.ts
  - apps/api/src/auth/passwords.ts
  - apps/api/src/auth/session-policy.ts
  - apps/api/src/__tests__/auth/passwords.spec.ts
  - apps/api/src/__tests__/auth/session-policy.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - OPS-04
must_haves:
  truths:
    - "The API owns a deterministic auth schema and can bootstrap it against PostgreSQL."
    - "Password hashing, session-duration rules, and auth data contracts are centralized before controllers are added."
    - "Phase 2 session semantics are encoded once and are compatible with future Phase 3 session inventory."
  artifacts:
    - path: "apps/api/src/db/migrations/0001_auth_core.sql"
      provides: "Initial auth schema for users, sessions, reset tokens, and mock-mail artifacts metadata if needed"
    - path: "apps/api/src/auth/passwords.ts"
      provides: "Central password-hash and verification boundary"
    - path: "apps/api/src/auth/session-policy.ts"
      provides: "Locked Phase 2 session-duration policy helper"
  key_links:
    - from: "apps/api/src/db/db.module.ts"
      to: "packages/shared/src/config.ts"
      via: "shared Postgres runtime config"
      pattern: "POSTGRES_"
    - from: "apps/api/src/auth/session-policy.ts"
      to: ".planning/phases/02-authentication-core/02-CONTEXT.md"
      via: "locked persistence rules"
      pattern: "24|30-day|Keep me signed in"
    - from: "apps/api/src/auth/passwords.ts"
      to: "apps/api/src/__tests__/auth/passwords.spec.ts"
      via: "security helper coverage"
      pattern: "hash|verify"
---

<objective>
Create the persistence and security primitives that all later Phase 2 work depends on.

Purpose: introduce deterministic PostgreSQL auth schema/bootstrap, centralized password-hash helpers, and locked session-duration rules before any registration or sign-in endpoints exist.
Output: DB module/bootstrap path, initial auth SQL migration, password/session helper modules, and targeted tests around the non-UI security rules.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-core/02-CONTEXT.md
@.planning/phases/02-authentication-core/02-RESEARCH.md
@packages/shared/src/config.ts
@infra/compose/compose.yaml

<interfaces>
Schema and helper boundaries created here must support:

- users with unique email and unique immutable username
- durable browser sessions with per-session expiry metadata
- password reset tokens for email-link flows
- session policy:
  - no keep-signed-in -> browser-close or 24h cap
  - keep-signed-in -> 30-day idle timeout

This plan does not create HTTP auth endpoints yet.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the PostgreSQL auth schema and DB bootstrap path</name>
  <read_first>.planning/phases/02-authentication-core/02-CONTEXT.md, .planning/phases/02-authentication-core/02-RESEARCH.md, packages/shared/src/config.ts, apps/api/src/app.module.ts, infra/compose/compose.yaml</read_first>
  <files>apps/api/package.json, packages/shared/src/config.ts, apps/api/src/db/postgres.service.ts, apps/api/src/db/db.module.ts, apps/api/src/db/migrations/0001_auth_core.sql, apps/api/src/auth/auth.types.ts</files>
  <acceptance_criteria>`0001_auth_core.sql` defines durable tables and indexes for users, sessions, and password-reset tokens; API-side DB bootstrap code reads Postgres config from shared runtime env; schema ownership is deterministic and checked into the repo; username is modeled as immutable-by-API rather than a mutable profile field.</acceptance_criteria>
  <behavior>
    - Schema supports unique email and unique username.
    - Session rows can distinguish persistent vs non-persistent session policy and track expiry/last-seen timestamps needed later.
    - Reset-token storage supports one-time consumption of emailed reset links.
  </behavior>
  <action>Create the initial auth persistence layer inside `apps/api`: a small DB module/service, checked-in SQL migration/bootstrap for auth tables, and auth-domain types that later controllers and services can reuse. Keep this plan focused on persistence primitives and data contracts; do not add controllers, cookies, or frontend work yet. Preserve compatibility with Phase 3 by storing per-session records instead of adopting stateless-only auth.</action>
  <verify>
    <automated>rg -n "create table|users|sessions|password_reset|unique|username|email" apps/api/src/db/migrations/0001_auth_core.sql && rg -n "POSTGRES_HOST|POSTGRES_PORT|POSTGRES_DB|POSTGRES_USER|POSTGRES_PASSWORD" packages/shared/src/config.ts apps/api/src/db/postgres.service.ts apps/api/src/db/db.module.ts</automated>
  </verify>
  <done>The repo owns a deterministic auth schema/bootstrap path and later plans can depend on stable auth persistence primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize password and session policy helpers</name>
  <read_first>.planning/phases/02-authentication-core/02-CONTEXT.md, .planning/phases/02-authentication-core/02-RESEARCH.md, apps/api/src/auth/passwords.ts, apps/api/src/auth/session-policy.ts</read_first>
  <files>apps/api/src/auth/passwords.ts, apps/api/src/auth/session-policy.ts, apps/api/src/__tests__/auth/passwords.spec.ts, apps/api/src/__tests__/auth/session-policy.spec.ts</files>
  <acceptance_criteria>Password hashing and verification live behind one helper boundary; session policy helper encodes both Phase 2 duration modes exactly; helper tests cover the locked session-duration rules and prevent drift.</acceptance_criteria>
  <behavior>
    - Password helper produces secure stored hashes and verifies them without exposing raw algorithm details to controllers.
    - Session policy helper returns the correct cookie/session semantics for both `Keep me signed in` states.
    - Helper code is reusable by registration, sign-in, reset, and password-change flows.
  </behavior>
  <action>Implement centralized security helpers for password operations and session-duration rules. The session policy helper must encode the exact user-approved Phase 2 semantics so later plans do not duplicate or reinterpret them in controllers or the web app. Add focused tests for these helpers before the endpoint work begins.</action>
  <verify>
    <automated>rg -n "24|30|keep|persistent|idle|browser" apps/api/src/auth/session-policy.ts apps/api/src/__tests__/auth/session-policy.spec.ts && rg -n "hash|verify|password" apps/api/src/auth/passwords.ts apps/api/src/__tests__/auth/passwords.spec.ts</automated>
  </verify>
  <done>Security-sensitive auth rules are centralized, tested, and ready for endpoint integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> PostgreSQL | Auth integrity depends on durable schema and safe DB access patterns |
| plaintext password -> stored hash | Raw credentials cross a high-risk boundary and must never be stored or logged |
| session policy helper -> later controllers | Phase-locked duration rules must not drift when HTTP handlers are added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T/I | auth SQL schema | mitigate | Check in one deterministic auth schema/bootstrap path with unique constraints and explicit session/reset-token tables. |
| T-02-02 | I | password helper boundary | mitigate | Centralize hashing and verification so raw passwords never leak into storage or logs. |
| T-02-03 | T | session policy rules | mitigate | Encode approved Phase 2 session semantics in one helper with tests so later handlers cannot silently drift. |
</threat_model>

<verification>
Run the grep checks above, then confirm schema, DB bootstrap, and helper tests exist before registration/sign-in endpoint work starts.
</verification>

<success_criteria>
Phase 2 has deterministic auth persistence primitives and centralized security/session helpers that later plans can consume without redefining the rules.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-core/02-authentication-core-01-SUMMARY.md`
</output>

