---
phase: 02-authentication-core
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - apps/api/src/auth/password-reset.service.ts
  - apps/api/src/auth/password-reset.controller.ts
  - apps/api/src/auth/change-password.service.ts
  - apps/api/src/mail/mock-mail.service.ts
  - apps/api/src/mail/mail.module.ts
  - apps/api/src/__tests__/auth/password-reset.spec.ts
  - apps/api/src/__tests__/auth/change-password.spec.ts
  - infra/compose/compose.yaml
  - docs/offline-runtime.md
autonomous: true
requirements:
  - AUTH-06
  - AUTH-07
  - OPS-04
must_haves:
  truths:
    - "Password reset uses an emailed reset link and a server-side one-time token record."
    - "Logged-in users can change password through an authenticated backend flow."
    - "Mock mail is SMTP-free, filesystem-backed, and QA-discoverable by logs."
  artifacts:
    - path: "apps/api/src/auth/password-reset.controller.ts"
      provides: "Reset-request and reset-confirm HTTP surface"
    - path: "apps/api/src/mail/mock-mail.service.ts"
      provides: "Filesystem-backed structured mail artifacts"
    - path: "infra/compose/compose.yaml"
      provides: "Writable mail-outbox mount while preserving read-only container posture elsewhere"
  key_links:
    - from: "apps/api/src/auth/password-reset.service.ts"
      to: "apps/api/src/mail/mock-mail.service.ts"
      via: "reset-link mail generation"
      pattern: "reset|mail|outbox"
    - from: "apps/api/src/auth/change-password.service.ts"
      to: "apps/api/src/auth/current-user.guard.ts"
      via: "authenticated password-change flow"
      pattern: "current user|password"
    - from: "infra/compose/compose.yaml"
      to: "apps/api/src/mail/mock-mail.service.ts"
      via: "mounted filesystem path"
      pattern: "mail|outbox|volume"
---

<objective>
Implement password reset, password change, and the filesystem-backed mock mail outbox.

Purpose: satisfy `AUTH-06`, `AUTH-07`, and `OPS-04` without introducing real SMTP or weakening the offline/read-only runtime constraints.
Output: reset-link backend flows, authenticated password-change flow, structured outbox mail artifacts, compose mount updates, and targeted tests.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-core/02-CONTEXT.md
@.planning/phases/02-authentication-core/02-RESEARCH.md
@.planning/phases/02-authentication-core/02-02-PLAN.md
@infra/compose/compose.yaml
@docs/offline-runtime.md

<interfaces>
Implement stable Phase 2 backend surfaces for:

- `POST /api/v1/auth/password-reset/request`
- `POST /api/v1/auth/password-reset/confirm`
- authenticated password-change endpoint

Reset-request writes a structured mail artifact to a mounted local directory and logs the artifact path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build password reset request/confirm with reset-link outbox artifacts</name>
  <read_first>.planning/phases/02-authentication-core/02-CONTEXT.md, .planning/phases/02-authentication-core/02-RESEARCH.md, infra/compose/compose.yaml, apps/api/src/auth/password-reset.service.ts, apps/api/src/mail/mock-mail.service.ts</read_first>
  <files>apps/api/src/auth/password-reset.service.ts, apps/api/src/auth/password-reset.controller.ts, apps/api/src/mail/mock-mail.service.ts, apps/api/src/mail/mail.module.ts, apps/api/src/__tests__/auth/password-reset.spec.ts, infra/compose/compose.yaml, docs/offline-runtime.md</files>
  <acceptance_criteria>Reset request creates/updates a one-time reset token record and writes a structured mail file containing the reset-link artifact; reset confirm consumes the token and updates the stored password hash; API logs expose the mail artifact path; compose provides a narrow writable mount for the outbox directory.</acceptance_criteria>
  <behavior>
    - Reset flow uses an emailed link, not a code.
    - Token consumption is one-time and server-validated.
    - Mail stays local-only and inspectable by QA without SMTP.
  </behavior>
  <action>Implement the reset-link flow and mock mail outbox path as Phase 2’s mail solution. Treat mock mail as structured artifact generation, not as an SMTP simulation service. Update compose and any related runtime docs/config only as needed to support a narrow writable outbox mount while preserving the general read-only API container posture.</action>
  <verify>
    <automated>rg -n "password-reset|reset|link|mail|outbox|path" apps/api/src/auth/password-reset.service.ts apps/api/src/auth/password-reset.controller.ts apps/api/src/mail/mock-mail.service.ts apps/api/src/__tests__/auth/password-reset.spec.ts infra/compose/compose.yaml docs/offline-runtime.md</automated>
  </verify>
  <done>Password reset works through reset-link artifacts that QA can inspect locally without external mail infrastructure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authenticated password-change flow</name>
  <read_first>.planning/phases/02-authentication-core/02-CONTEXT.md, .planning/phases/02-authentication-core/02-RESEARCH.md, apps/api/src/auth/change-password.service.ts, apps/api/src/auth/current-user.guard.ts</read_first>
  <files>apps/api/src/auth/change-password.service.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.service.ts, apps/api/src/__tests__/auth/change-password.spec.ts</files>
  <acceptance_criteria>Authenticated users can change password by presenting current password and new password; old password is verified before update; password change reuses the centralized password helper and authenticated session plumbing.</acceptance_criteria>
  <behavior>
    - Password change is available only to an authenticated current session.
    - Current password must be verified before replacing the stored hash.
    - Flow stays separate from reset-token behavior.
  </behavior>
  <action>Implement a dedicated authenticated password-change backend flow using the existing auth guard/current-user boundary and password helper from earlier plans. Keep this surface minimal and Phase 2-specific; do not add profile editing or account deletion here.</action>
  <verify>
    <automated>rg -n "change password|current password|new password|verify" apps/api/src/auth/change-password.service.ts apps/api/src/auth/auth.controller.ts apps/api/src/__tests__/auth/change-password.spec.ts</automated>
  </verify>
  <done>The backend covers both password recovery and authenticated password updates without scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reset-request -> mail artifact | Sensitive reset-link material crosses into a QA-visible artifact |
| authenticated session -> password-change handler | Only valid current sessions may change password |
| read-only API container -> writable outbox path | Compose must grant only the minimum write surface needed for mock mail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | I | reset-link artifacts | mitigate | Keep reset-token validation server-side and write only structured local artifacts with explicit path logging. |
| T-02-08 | S/T | password-change flow | mitigate | Require authenticated current session plus current-password verification before hash replacement. |
| T-02-09 | T/E | API container write surface | mitigate | Add one narrow mail-outbox mount instead of weakening the entire container filesystem posture. |
</threat_model>

<verification>
Run the grep checks above, then confirm reset-request/reset-confirm, password change, and filesystem mail artifacts exist before web UX work begins.
</verification>

<success_criteria>
Phase 2 can recover passwords through reset links, change passwords for logged-in users, and expose QA-visible mock mail artifacts without real SMTP.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-core/02-authentication-core-03-SUMMARY.md`
</output>

