---
phase: 02-authentication-core
plan: 04
type: execute
wave: 4
depends_on:
  - 02
  - 03
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/main.tsx
  - apps/web/src/styles.css
  - apps/web/src/lib/api.ts
  - apps/web/src/features/auth/AuthShell.tsx
  - apps/web/src/features/auth/SignInView.tsx
  - apps/web/src/features/auth/RegisterView.tsx
  - apps/web/src/features/auth/ForgotPasswordView.tsx
  - apps/web/src/features/account/PasswordSettingsView.tsx
  - apps/web/src/features/account/SessionActionsView.tsx
  - scripts/qa/phase2-auth-smoke.sh
  - .planning/phases/02-authentication-core/02-VALIDATION.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
must_haves:
  truths:
    - "The web app ships the locked Phase 2 auth shell instead of the Phase 1 placeholder shell."
    - "Register and forgot-password are separate views within the same auth shell, reachable from the default sign-in view."
    - "The logged-in surface exposes only the minimum Phase 2 account actions: password change and current-session sign-out."
  artifacts:
    - path: "apps/web/src/features/auth/AuthShell.tsx"
      provides: "Single auth shell that switches between sign-in/register/forgot-password views"
    - path: "apps/web/src/features/account/PasswordSettingsView.tsx"
      provides: "Minimal logged-in password-change UX"
    - path: "scripts/qa/phase2-auth-smoke.sh"
      provides: "Phase 2 auth smoke coverage"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/features/auth/AuthShell.tsx"
      via: "Phase 2 app entry"
      pattern: "auth|sign in|register|forgot"
    - from: "apps/web/src/features/auth/SignInView.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "sign-in/register/reset API calls"
      pattern: "signIn|register|passwordReset"
    - from: "apps/web/src/features/account/SessionActionsView.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "current-session sign-out"
      pattern: "signOut|me"
---

<objective>
Ship the Phase 2 web UX and the validation loop that proves the auth slice works end-to-end.

Purpose: replace the Phase 1 web placeholder with the locked auth shell, expose minimal logged-in account actions, and create the Phase 2 validation/smoke contract.
Output: React auth/account views, API client glue, phase-specific smoke script, and `02-VALIDATION.md`.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-core/02-CONTEXT.md
@.planning/phases/02-authentication-core/02-RESEARCH.md
@requirements/wireframes.md
@requirements/desing_v1/components/auth.jsx
@requirements/desing_v1/components/account.jsx
@apps/web/src/App.tsx
@apps/web/src/styles.css

<interfaces>
The shipped Phase 2 UX must preserve:

- default sign-in view
- register and forgot-password as switchable dedicated views inside the same auth shell
- visible `Keep me signed in`
- authenticated password-change surface
- current-session sign-out only

Do not add active-session inventory, sign-out-all, classic chat shell navigation, rooms, or messaging UI in this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the Phase 1 shell with the locked Phase 2 auth shell</name>
  <read_first>.planning/phases/02-authentication-core/02-CONTEXT.md, requirements/wireframes.md, requirements/desing_v1/components/auth.jsx, apps/web/src/App.tsx, apps/web/src/styles.css</read_first>
  <files>apps/web/src/App.tsx, apps/web/src/main.tsx, apps/web/src/styles.css, apps/web/src/lib/api.ts, apps/web/src/features/auth/AuthShell.tsx, apps/web/src/features/auth/SignInView.tsx, apps/web/src/features/auth/RegisterView.tsx, apps/web/src/features/auth/ForgotPasswordView.tsx</files>
  <acceptance_criteria>The web app defaults to a sign-in card shell; register and forgot-password are dedicated switchable views in the same shell; the sign-in form exposes `Keep me signed in`; API integration paths exist for register/sign-in/reset request; the Phase 1 placeholder copy no longer defines the shipped product UI.</acceptance_criteria>
  <action>Implement the shipped auth shell in React using the locked Phase 2 direction from context and the prototype as a visual reference, not as direct runtime code. Preserve the single-screen auth shell with switches between sign-in, register, and forgot-password views. Keep styling intentional but within the established product direction. Do not add full application navigation or rooms/messaging UI.</action>
  <verify>
    <automated>rg -n "Sign in|Register|Forgot password|Keep me signed in" apps/web/src/App.tsx apps/web/src/features/auth/AuthShell.tsx apps/web/src/features/auth/SignInView.tsx apps/web/src/features/auth/RegisterView.tsx apps/web/src/features/auth/ForgotPasswordView.tsx</automated>
  </verify>
  <done>The shipped web app presents the locked Phase 2 auth UX instead of the foundation placeholder shell.</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal logged-in account actions and Phase 2 validation assets</name>
  <read_first>.planning/phases/02-authentication-core/02-CONTEXT.md, requirements/desing_v1/components/account.jsx, apps/web/src/lib/api.ts, .planning/phases/02-authentication-core/02-VALIDATION.md</read_first>
  <files>apps/web/src/features/account/PasswordSettingsView.tsx, apps/web/src/features/account/SessionActionsView.tsx, apps/web/src/App.tsx, scripts/qa/phase2-auth-smoke.sh, .planning/phases/02-authentication-core/02-VALIDATION.md</files>
  <acceptance_criteria>After auth, the web app exposes password change and current-session sign-out only; Phase 2 smoke script covers register/sign-in/sign-out/reset request at a high level; `02-VALIDATION.md` maps every Phase 2 task to an automated or smoke verification path with no unresolved placeholders.</acceptance_criteria>
  <action>Add the minimum logged-in UI surface needed for Phase 2 completion: password-change and current-session sign-out. Then create the phase validation contract and one auth smoke script that covers the core user journeys without reaching into future session-management scope. Keep the logged-in UI intentionally small; active-session inventory and sign-out-all remain deferred to Phase 3.</action>
  <verify>
    <automated>rg -n "password|sign out|current session" apps/web/src/features/account/PasswordSettingsView.tsx apps/web/src/features/account/SessionActionsView.tsx apps/web/src/App.tsx && test -f scripts/qa/phase2-auth-smoke.sh && rg -n "02-01-01|02-01-02|02-02-01|02-02-02|02-03-01|02-03-02|02-04-01|02-04-02|T-02-01|T-02-09" .planning/phases/02-authentication-core/02-VALIDATION.md</automated>
  </verify>
  <done>Phase 2 closes with shipped auth/account UX and a concrete validation loop for the slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser forms -> auth API | User-entered credentials and reset requests leave the web client |
| logged-in UI -> current-session sign-out | The frontend must not accidentally expose broader session management than Phase 2 allows |
| smoke harness -> shipped auth slice | Validation must test shipped behavior, not internal shortcuts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | T | auth shell UX scope | mitigate | Keep the shipped web UX constrained to sign-in/register/forgot-password plus minimal account actions. |
| T-02-11 | S/T | logged-in web actions | mitigate | Expose only current-session sign-out and password change, leaving multi-session surfaces for Phase 3. |
| T-02-12 | D | phase validation loop | mitigate | Create a dedicated auth smoke script and validation map so the slice can be exercised end-to-end before later phases build on it. |
</threat_model>

<verification>
Run the grep checks above, then use `scripts/qa/phase2-auth-smoke.sh` plus the validation map as the Phase 2 acceptance loop.
</verification>

<success_criteria>
The web app ships the locked Phase 2 auth/account UX and the phase has a concrete automated validation path for its critical flows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-core/02-authentication-core-04-SUMMARY.md`
</output>

