---
phase: 02-authentication-core
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - apps/api/package.json
  - apps/api/src/app.module.ts
  - apps/api/src/auth/auth.module.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/auth/session-cookie.ts
  - apps/api/src/auth/current-user.guard.ts
  - apps/api/src/auth/current-user.decorator.ts
  - apps/api/src/auth/session.repository.ts
  - apps/api/src/auth/user.repository.ts
  - apps/api/src/__tests__/auth/register-login.spec.ts
  - apps/api/src/__tests__/auth/logout.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - "Registration enforces unique email and immutable username rules."
    - "Sign-in creates a durable per-browser session and delivers the correct cookie policy."
    - "Current-session sign-out invalidates only the active browser session."
  artifacts:
    - path: "apps/api/src/auth/auth.controller.ts"
      provides: "HTTP auth surface for register, sign-in, sign-out, and current-user lookup"
    - path: "apps/api/src/auth/session-cookie.ts"
      provides: "One canonical cookie issuance/clearing policy"
    - path: "apps/api/src/auth/session.repository.ts"
      provides: "Durable session persistence boundary"
  key_links:
    - from: "apps/api/src/auth/auth.controller.ts"
      to: "apps/api/src/auth/auth.service.ts"
      via: "registration and sign-in orchestration"
      pattern: "register|sign-in|sign-out|me"
    - from: "apps/api/src/auth/auth.service.ts"
      to: "apps/api/src/auth/session-policy.ts"
      via: "locked cookie/session durations"
      pattern: "keepSignedIn|24|30"
    - from: "apps/api/src/auth/session-cookie.ts"
      to: "apps/api/src/auth/current-user.guard.ts"
      via: "session cookie extraction and auth lookup"
      pattern: "cookie|session"
---

<objective>
Implement the backend registration, sign-in, current-user, and current-session sign-out flows.

Purpose: satisfy the core account lifecycle and durable-login requirements with stable HTTP endpoints and per-browser session records.
Output: auth module, repositories/services/controllers, cookie/session helpers, and backend tests for registration/sign-in/logout behavior.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication-core/02-CONTEXT.md
@.planning/phases/02-authentication-core/02-RESEARCH.md
@.planning/phases/02-authentication-core/02-01-PLAN.md
@apps/api/src/main.ts
@apps/api/src/app.module.ts

<interfaces>
Implement stable Phase 2 auth endpoints and semantics for:

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/sign-in`
- `POST /api/v1/auth/sign-out`
- `GET /api/v1/auth/me`

Sign-out must invalidate only the current browser session.
Username must remain immutable after registration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build registration and sign-in with durable session issuance</name>
  <read_first>.planning/phases/02-authentication-core/02-CONTEXT.md, .planning/phases/02-authentication-core/02-RESEARCH.md, .planning/phases/02-authentication-core/02-01-PLAN.md, apps/api/src/auth/auth.module.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.service.ts</read_first>
  <files>apps/api/package.json, apps/api/src/app.module.ts, apps/api/src/auth/auth.module.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.service.ts, apps/api/src/auth/user.repository.ts, apps/api/src/auth/session.repository.ts, apps/api/src/auth/session-cookie.ts, apps/api/src/__tests__/auth/register-login.spec.ts</files>
  <acceptance_criteria>Registration persists users with unique email/username handling; sign-in verifies password and issues one per-browser session record plus cookie; current-user lookup can resolve the session; username is not exposed as an updatable field in these flows.</acceptance_criteria>
  <behavior>
    - Registration rejects duplicate email and duplicate username deterministically.
    - Sign-in uses the approved `Keep me signed in` policy to create the correct session shape.
    - Authenticated current-user lookup returns only the required user/auth state, not password hashes or internal secrets.
  </behavior>
  <action>Implement the Phase 2 auth module and repositories/services/controllers for registration, sign-in, and current-user lookup. Use the Plan 01 schema and helpers directly; do not duplicate session-duration logic or password verification inside controllers. Keep the HTTP surface narrow and phase-scoped: no active-session inventory, no sign-out-all, no profile editing beyond what Phase 2 requires.</action>
  <verify>
    <automated>rg -n "register|sign-in|me|duplicate|username|email|session" apps/api/src/auth/auth.controller.ts apps/api/src/auth/auth.service.ts apps/api/src/auth/user.repository.ts apps/api/src/auth/session.repository.ts apps/api/src/__tests__/auth/register-login.spec.ts</automated>
  </verify>
  <done>The API can create users, authenticate them, and resolve the current browser session using durable session records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement current-session sign-out and auth guard/decorator plumbing</name>
  <read_first>.planning/phases/02-authentication-core/02-CONTEXT.md, .planning/phases/02-authentication-core/02-RESEARCH.md, apps/api/src/auth/session-cookie.ts, apps/api/src/auth/current-user.guard.ts, apps/api/src/auth/current-user.decorator.ts</read_first>
  <files>apps/api/src/auth/session-cookie.ts, apps/api/src/auth/current-user.guard.ts, apps/api/src/auth/current-user.decorator.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.service.ts, apps/api/src/__tests__/auth/logout.spec.ts</files>
  <acceptance_criteria>`POST /api/v1/auth/sign-out` invalidates only the current browser session; cookie clearing and session lookup are centralized; authenticated handlers can access the current user/session through one shared guard/decorator path.</acceptance_criteria>
  <behavior>
    - Sign-out deletes or invalidates only the presented session token.
    - Other session rows remain untouched for future Phase 3 management.
    - Protected handlers reuse one guard/decorator boundary rather than ad hoc session parsing.
  </behavior>
  <action>Implement the current-session sign-out flow and the minimal auth guard/decorator infrastructure needed by `me`, password-change, and later Phase 3 handlers. Ensure this plan preserves the future session-management surface by invalidating only the current session token. Do not add bulk session management or active-session listing here.</action>
  <verify>
    <automated>rg -n "sign-out|cookie|guard|CurrentUser|invalidate|session" apps/api/src/auth/session-cookie.ts apps/api/src/auth/current-user.guard.ts apps/api/src/auth/current-user.decorator.ts apps/api/src/auth/auth.controller.ts apps/api/src/auth/auth.service.ts apps/api/src/__tests__/auth/logout.spec.ts</automated>
  </verify>
  <done>Phase 2 has a durable-login backend with targeted current-session sign-out and reusable authenticated-request plumbing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> auth HTTP endpoints | Public account creation and sign-in surface |
| session cookie -> protected handlers | Cookie parsing/auth lookup controls access to authenticated APIs |
| one session row -> many future sessions | Current-session operations must not accidentally broaden into global logout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | S/T | auth registration/sign-in surface | mitigate | Validate uniqueness and credentials server-side and centralize session issuance behind one service boundary. |
| T-02-05 | T/E | session cookie handling | mitigate | Use one canonical cookie helper and shared auth guard rather than ad hoc token parsing in controllers. |
| T-02-06 | T | current-session sign-out | mitigate | Invalidate only the presented session row so unrelated sessions remain valid for Phase 3. |
</threat_model>

<verification>
Run the grep checks above, then confirm registration, sign-in, current-user, and current-session sign-out all exist before reset/change-password work begins.
</verification>

<success_criteria>
The backend can create accounts, issue durable browser sessions with the locked policy, resolve the current user, and sign out only the current session.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-core/02-authentication-core-02-SUMMARY.md`
</output>

