---
phase: 01-foundation-and-offline-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/styles.css
  - apps/web/index.html
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/public/healthz
  - apps/web/public/assets/prototype-reference/README.md
autonomous: true
requirements:
  - OPS-02
  - ARCH-02
must_haves:
  truths:
    - "The browser client is served from locally built assets rather than CDN scripts or hosted fonts."
    - "The web container exposes a deterministic health target that Compose can verify."
    - "The frontend proves the mixed transport boundary by knowing both the REST and WebSocket endpoints."
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "Local Phase 1 shell rendered by bundled React assets"
    - path: "apps/web/vite.config.ts"
      provides: "Build configuration for local asset bundling"
    - path: "apps/web/public/healthz"
      provides: "Static readiness endpoint for the web container"
  key_links:
    - from: "apps/web/src/main.tsx"
      to: "apps/web/src/App.tsx"
      via: "React bootstrap"
      pattern: "createRoot"
    - from: "apps/web/src/App.tsx"
      to: "packages/shared/src/config.ts"
      via: "runtime endpoint display"
      pattern: "SERVICE_PORTS"
    - from: "apps/web/vite.config.ts"
      to: "apps/web/public/healthz"
      via: "served static asset"
      pattern: "publicDir|healthz"
---

<objective>
Replace the prototype's runtime internet dependency path with a locally built frontend shell that proves bundled asset delivery.

Purpose: satisfy the offline asset portion of OPS-02 and give Compose a real `web` service target without smuggling product features into Phase 1.
Output: `apps/web` Vite app, local HTML/CSS/React entrypoint, health asset, and a minimal shell that advertises REST/WebSocket endpoints only.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-offline-delivery/01-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/codebase/ARCHITECTURE.md
@requirements/desing_v1/index.html

<interfaces>
Consume the shared contract created in Plan 01:

```ts
import { SERVICE_PORTS } from "@chat/shared";
```

The web shell should expose only these Phase 1 facts:
- API base URL
- WebSocket namespace/path
- offline asset delivery statement
- no chat/auth/product flows yet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the local web build pipeline</name>
  <read_first>.planning/phases/01-foundation-and-offline-delivery/01-RESEARCH.md, apps/web/package.json, apps/web/vite.config.ts, apps/web/index.html, apps/web/tsconfig.json</read_first>
  <files>apps/web/vite.config.ts, apps/web/index.html, apps/web/tsconfig.json</files>
  <acceptance_criteria>`apps/web/index.html` contains no `unpkg.com`, `fonts.googleapis.com`, or runtime Babel tags; `apps/web/vite.config.ts` defines a buildable Vite config; `apps/web/tsconfig.json` extends the root TypeScript config created in Plan 01.</acceptance_criteria>
  <action>Create the Phase 1 frontend build scaffolding under `apps/web` using Vite and TypeScript. Replace the prototype-era CDN script assumptions with a normal local entrypoint, and keep the config minimal: one SPA HTML shell, one build output, no router, no auth, no room or messaging state. Preserve the prototype only as reference material under `requirements/`; do not import it into runtime code.</action>
  <verify>
    <automated>rg -n "unpkg|fonts.googleapis|babel" apps/web/index.html && exit 1 || true; rg -n "defineConfig|extends" apps/web/vite.config.ts apps/web/tsconfig.json</automated>
  </verify>
  <done>The frontend can be built from repo-local source code without browser-time internet fetches.</done>
</task>

<task type="auto">
  <name>Task 2: Build a minimal offline web shell and health target</name>
  <read_first>.planning/research/PITFALLS.md, packages/shared/src/config.ts, apps/web/src/main.tsx, apps/web/src/App.tsx, apps/web/src/styles.css, apps/web/public/healthz</read_first>
  <files>apps/web/src/main.tsx, apps/web/src/App.tsx, apps/web/src/styles.css, apps/web/public/healthz, apps/web/public/assets/prototype-reference/README.md</files>
  <acceptance_criteria>`apps/web/src/App.tsx` renders a Phase 1 status page that references both HTTP and WebSocket transport endpoints from shared config; `apps/web/public/healthz` exists; `apps/web/src/styles.css` contains only local styling rules and no `@import` to hosted assets.</acceptance_criteria>
  <action>Implement a deliberately small frontend shell: render a page that states this is the local/offline build, shows the planned API and WebSocket endpoints from `@chat/shared`, and confirms assets are bundled locally. Add `apps/web/public/healthz` for container readiness and a short `apps/web/public/assets/prototype-reference/README.md` noting that the old prototype remains a design reference only. Do not add navigation, rooms, auth forms, or fake message UI in this phase.</action>
  <verify>
    <automated>rg -n "SERVICE_PORTS|WebSocket|offline|Phase 1" apps/web/src/App.tsx apps/web/src/main.tsx && test -f apps/web/public/healthz && ! rg -n "@import|fonts.googleapis|unpkg" apps/web/src/styles.css apps/web/index.html</automated>
  </verify>
  <done>The shipped frontend path is local-only, bundle-based, and ready for containerization and startup smoke checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> web static assets | Untrusted browser must only receive repo-built files, not remote runtime assets |
| web shell -> API/WebSocket endpoints | Frontend declares transport boundaries that later services will expose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | I/D | web HTML and styles | mitigate | Remove CDN scripts, remote fonts, and runtime Babel so the frontend has no hidden runtime internet dependency. |
| T-01-05 | S/T | web shell transport declarations | mitigate | Limit the Phase 1 UI to endpoint/status display only, avoiding any unauthenticated product behavior beyond proving REST/WebSocket boundary existence. |
| T-01-06 | D | web readiness target | mitigate | Add a static `/healthz` asset so Compose can verify the web container instead of guessing on startup timing. |
</threat_model>

<verification>
Run the task-level grep checks, then ensure the built frontend source tree contains no CDN/font/Babel references before Compose wiring begins.
</verification>

<success_criteria>
The frontend no longer depends on the browser-downloaded prototype stack and exposes only the minimal local asset and transport proof required for Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-offline-delivery/01-foundation-and-offline-delivery-02-SUMMARY.md`
</output>
