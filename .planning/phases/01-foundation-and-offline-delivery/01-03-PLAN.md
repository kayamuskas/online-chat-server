---
phase: 01-foundation-and-offline-delivery
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - apps/worker/package.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/health/health.controller.ts
  - apps/api/src/meta/meta.controller.ts
  - apps/api/src/ws/app.gateway.ts
  - apps/api/src/queue/queue.module.ts
  - apps/api/src/queue/system-jobs.controller.ts
  - apps/api/tsconfig.json
  - apps/worker/src/main.ts
  - apps/worker/src/system.worker.ts
  - apps/worker/tsconfig.json
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "The backend boots as a mixed REST and WebSocket service."
    - "The API exposes a deterministic enqueue path for a trivial system job that the worker can consume."
    - "A separate worker process boots against Redis and can process the trivial system job."
    - "The API exposes health and metadata endpoints that later Compose and QA checks can depend on."
  artifacts:
    - path: "apps/api/src/ws/app.gateway.ts"
      provides: "Initial WebSocket boundary"
    - path: "apps/api/src/queue/queue.module.ts"
      provides: "BullMQ queue registration in the API"
    - path: "apps/api/src/queue/system-jobs.controller.ts"
      provides: "Deterministic API enqueue path for the Phase 1 system job"
    - path: "apps/worker/src/system.worker.ts"
      provides: "Worker-side BullMQ consumer"
  key_links:
    - from: "apps/api/src/main.ts"
      to: "apps/api/src/app.module.ts"
      via: "Nest bootstrap"
      pattern: "NestFactory"
    - from: "apps/api/src/queue/queue.module.ts"
      to: "packages/shared/src/queue.ts"
      via: "shared queue constants"
      pattern: "QUEUE_NAMES"
    - from: "apps/api/src/queue/system-jobs.controller.ts"
      to: "packages/shared/src/queue.ts"
      via: "fixed queue and job-name contract"
      pattern: "SYSTEM_JOB_NAMES|QUEUE_NAMES"
    - from: "apps/worker/src/system.worker.ts"
      to: "redis"
      via: "BullMQ Worker connection"
      pattern: "maxRetriesPerRequest: null"
---

<objective>
Bootstrap the API and worker runtimes that establish the REST/WebSocket boundary and queue foundation for later phases.

Purpose: satisfy ARCH-01 and ARCH-02 with real Nest and BullMQ entrypoints while keeping the surface area limited to health, metadata, handshake, and system-job plumbing.
Output: `apps/api` Nest bootstrap, health/meta controllers, WebSocket gateway, queue module, and `apps/worker` BullMQ worker bootstrap.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-offline-delivery/01-RESEARCH.md
@.planning/phases/01-foundation-and-offline-delivery/01-VALIDATION.md
@.planning/research/ARCHITECTURE.md

<interfaces>
Use the Plan 01 shared contract and implement the following stable surfaces:

```ts
GET /healthz -> { status: "ok", service: "api" }
GET /api/v1/meta -> { service: "api", transports: ["rest", "websocket"], queues: ["system"] }
POST /api/v1/system-jobs/echo -> { enqueued: true, queue: "system", jobName: "echo" }
WebSocket namespace/path -> emits/returns a simple ready or pong payload only
Worker queue -> consumes `system` queue jobs with a noop/echo result
```

Worker Redis connections must use a BullMQ-safe policy:

```ts
{ host: process.env.REDIS_HOST, port: Number(process.env.REDIS_PORT), maxRetriesPerRequest: null }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap the Nest hybrid API surface</name>
  <read_first>.planning/phases/01-foundation-and-offline-delivery/01-RESEARCH.md, packages/shared/src/config.ts, packages/shared/src/queue.ts, apps/api/src/main.ts, apps/api/src/app.module.ts, apps/api/src/health/health.controller.ts, apps/api/src/meta/meta.controller.ts, apps/api/src/ws/app.gateway.ts, apps/api/src/queue/system-jobs.controller.ts</read_first>
  <files>apps/api/src/main.ts, apps/api/src/app.module.ts, apps/api/src/health/health.controller.ts, apps/api/src/meta/meta.controller.ts, apps/api/src/ws/app.gateway.ts, apps/api/src/queue/queue.module.ts, apps/api/src/queue/system-jobs.controller.ts, apps/api/tsconfig.json</files>
  <acceptance_criteria>`apps/api/src/main.ts` boots Nest on the shared API port; `GET /healthz` and `GET /api/v1/meta` are implemented in controllers; `apps/api/src/ws/app.gateway.ts` defines a gateway or namespace for a handshake/ping-only Phase 1 socket surface; `POST /api/v1/system-jobs/echo` exists as the deterministic enqueue path for queue smoke checks; the API queue module imports queue and job-name constants from `@chat/shared`.</acceptance_criteria>
  <behavior>
    - Test 1: HTTP health endpoint returns `{ status: "ok" }` without requiring auth.
    - Test 2: Metadata endpoint reports both `rest` and `websocket` transports plus the `system` queue.
    - Test 3: WebSocket handshake or ping route responds without exposing message or room actions.
    - Test 4: `POST /api/v1/system-jobs/echo` enqueues the fixed `system` job without exposing arbitrary queue or job-name input.
  </behavior>
  <action>Implement the Nest bootstrap with HTTP and Socket.IO in the same app, per the locked research decision for a mixed transport model. Keep the API surface intentionally narrow: health, metadata, a trivial gateway event, and one deterministic enqueue endpoint for the Phase 1 queue smoke test. Register one BullMQ queue module against the shared queue and job-name constants so the endpoint enqueues only the fixed `system` echo job consumed by the worker. Do not add auth guards, arbitrary job producers, domain modules, Postgres models, or messaging endpoints in this phase.</action>
  <verify>
    <automated>rg -n "NestFactory|healthz|api/v1/meta|system-jobs/echo|WebSocketGateway|QUEUE_NAMES|SYSTEM_JOB_NAMES" apps/api/src/main.ts apps/api/src/app.module.ts apps/api/src/health/health.controller.ts apps/api/src/meta/meta.controller.ts apps/api/src/ws/app.gateway.ts apps/api/src/queue/queue.module.ts apps/api/src/queue/system-jobs.controller.ts</automated>
  </verify>
  <done>The API has a real mixed-transport bootstrap with stable Phase 1 endpoints and no leaked domain behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap the BullMQ worker with safe Redis connection policy</name>
  <read_first>.planning/phases/01-foundation-and-offline-delivery/01-RESEARCH.md, .planning/phases/01-foundation-and-offline-delivery/01-VALIDATION.md, packages/shared/src/config.ts, packages/shared/src/queue.ts, apps/worker/src/main.ts, apps/worker/src/system.worker.ts</read_first>
  <files>apps/worker/package.json, apps/worker/src/main.ts, apps/worker/src/system.worker.ts, apps/worker/tsconfig.json</files>
  <acceptance_criteria>`apps/worker/package.json` and `apps/worker/tsconfig.json` define an independent worker runtime; `apps/worker/src/system.worker.ts` creates a BullMQ `Worker` for `QUEUE_NAMES.system`; the Redis connection sets `maxRetriesPerRequest: null`; the worker logs or reports successful processing of the fixed echo system job; no application-domain queue names are introduced.</acceptance_criteria>
  <behavior>
    - Test 1: Worker connects to Redis using the shared env contract and starts without throwing on BullMQ connection validation.
    - Test 2: The fixed `system` echo job enqueued by the API resolves successfully.
    - Test 3: Worker code remains isolated from auth, rooms, and messaging modules.
  </behavior>
  <action>Implement a separate worker package manifest and entrypoint that import the shared runtime config and queue constants, boot one BullMQ worker, and process the fixed noop/echo system job produced by the API enqueue endpoint. Use the BullMQ-safe Redis connection policy called out in the research: `maxRetriesPerRequest: null` for the worker path. Keep this process isolated from Nest HTTP code so Compose can run it independently in later plans.</action>
  <verify>
    <automated>rg -n "\"@chat/worker\"|new Worker|QUEUE_NAMES|SYSTEM_JOB_NAMES|maxRetriesPerRequest: null|system" apps/worker/package.json apps/worker/src/main.ts apps/worker/src/system.worker.ts</automated>
  </verify>
  <done>The repository has a separate worker runtime with BullMQ-safe Redis connectivity and a minimal queue consumer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API HTTP | Unauthenticated requests hit public health/meta routes |
| client -> WebSocket gateway | Browser opens an unauthenticated realtime connection |
| API/worker -> Redis | Queue and worker behavior depends on correct connection policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | S/E | `apps/api/src/ws/app.gateway.ts` | mitigate | Keep the unauthenticated WebSocket surface limited to handshake/ping-only behavior with no room, auth, or message actions in Phase 1. |
| T-01-08 | D | `apps/worker/src/system.worker.ts` | mitigate | Require BullMQ worker Redis connections to set `maxRetriesPerRequest: null` and use shared config so bad Redis/BullMQ worker connection policy is blocked. |
| T-01-09 | D | API bootstrap and queue registration | mitigate | Expose `/healthz` and `/api/v1/meta` so Compose and smoke tests can detect unhealthy startup instead of racing dependent services. |
</threat_model>

<verification>
Run the grep checks for controllers, gateway, and worker code. Plan 04 will add end-to-end transport and queue smoke scripts against these entrypoints.
</verification>

<success_criteria>
The backend foundation includes a real hybrid Nest app and an independent BullMQ worker, both constrained to Phase 1 bootstrap behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-offline-delivery/01-foundation-and-offline-delivery-03-SUMMARY.md`
</output>
