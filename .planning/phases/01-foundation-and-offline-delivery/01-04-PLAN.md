---
phase: 01-foundation-and-offline-delivery
plan: 04
type: execute
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - infra/compose/compose.yaml
  - infra/docker/api.Dockerfile
  - infra/docker/web.Dockerfile
  - infra/docker/worker.Dockerfile
  - scripts/qa/phase1-smoke.sh
  - scripts/qa/phase1-offline-check.sh
  - scripts/qa/phase1-queue-check.sh
  - scripts/qa/phase1-transport-check.sh
  - .planning/phases/01-foundation-and-offline-delivery/01-VALIDATION.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "QA can start the full stack with `docker compose up` using only repo-local sources and preloaded base images."
    - "Startup ordering waits for healthy dependencies instead of racing Postgres or Redis."
    - "Smoke checks prove offline startup, queue processing, and REST/WebSocket transport availability."
  artifacts:
    - path: "infra/compose/compose.yaml"
      provides: "Five-service topology with health-gated startup"
    - path: "scripts/qa/phase1-offline-check.sh"
      provides: "Offline dependency/startup smoke verification"
    - path: "scripts/qa/phase1-queue-check.sh"
      provides: "Queue foundation smoke verification"
  key_links:
    - from: "infra/compose/compose.yaml"
      to: "api"
      via: "depends_on.condition=service_healthy"
      pattern: "service_healthy"
    - from: "infra/docker/api.Dockerfile"
      to: "vendor/pnpm-store"
      via: "offline install path"
      pattern: "vendor/pnpm-store|--offline"
    - from: "scripts/qa/phase1-queue-check.sh"
      to: "POST /api/v1/system-jobs/echo"
      via: "deterministic queue smoke producer"
      pattern: "system-jobs/echo"
    - from: "scripts/qa/phase1-transport-check.sh"
      to: "/healthz and WebSocket handshake"
      via: "compose-driven smoke test"
      pattern: "healthz|websocket|socket"
---

<objective>
Package the Phase 1 services into an offline-capable Docker Compose topology and wire the smoke checks that prove the foundation works.

Purpose: satisfy OPS-01 and OPS-02 operationally while closing the queue and transport verification loop required by ARCH-01 and ARCH-02.
Output: Compose file, Dockerfiles, and QA smoke scripts for startup, offline behavior, queue behavior, and transport behavior.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-offline-delivery/01-RESEARCH.md
@.planning/phases/01-foundation-and-offline-delivery/01-VALIDATION.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@docs/offline-runtime.md

<interfaces>
Compose topology required by the phase prompt:
- `web`
- `api`
- `worker`
- `postgres`
- `redis`

Required operational rules:
- use `pull_policy: never` where image pulls must be blocked
- use `depends_on: { condition: service_healthy }` for `api`, `worker`, and `web` dependencies
- expose only the minimum ports needed for Phase 1 smoke checks
- mount filesystems read-only where writes are not required
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the offline Compose and Docker packaging layer</name>
  <read_first>.planning/phases/01-foundation-and-offline-delivery/01-RESEARCH.md, docs/offline-runtime.md, infra/compose/compose.yaml, infra/docker/api.Dockerfile, infra/docker/web.Dockerfile, infra/docker/worker.Dockerfile</read_first>
  <files>infra/compose/compose.yaml, infra/docker/api.Dockerfile, infra/docker/web.Dockerfile, infra/docker/worker.Dockerfile</files>
  <acceptance_criteria>`infra/compose/compose.yaml` defines exactly `web`, `api`, `worker`, `postgres`, and `redis`; `postgres` and `redis` have healthchecks; `api`, `worker`, and `web` use long-form `depends_on` with `condition: service_healthy`; local app Dockerfiles install dependencies with repo-local offline inputs; containers that do not need write access set `read_only: true` or the equivalent targeted writable mounts; the Dockerfiles and Compose wiring preserve the documented lockfile-to-vendored-store verification path from `docs/offline-runtime.md`.</acceptance_criteria>
  <action>Create the Phase 1 Compose topology under `infra/compose/compose.yaml` using the five required services. Add healthchecks and `service_healthy` startup ordering so the API and worker do not race unhealthy Postgres or Redis. Set `pull_policy: never` on services that should not trigger remote pulls during QA startup. Build `api`, `web`, and `worker` from local Dockerfiles that copy the workspace source and `vendor/pnpm-store` path, then run offline installs/builds only. Mark container filesystems read-only except for explicit writable paths that are operationally required, and keep the Docker build inputs aligned with the documented vendored-store refresh-and-verify procedure instead of introducing alternate dependency paths. Do not add mail, auth, room, or message services in this phase.</action>
  <verify>
    <automated>rg -n "^services:|pull_policy: never|service_healthy|read_only: true|vendor/pnpm-store|--offline" infra/compose/compose.yaml infra/docker/api.Dockerfile infra/docker/web.Dockerfile infra/docker/worker.Dockerfile</automated>
  </verify>
  <done>The repository contains a deterministic, offline-oriented container topology ready for QA smoke execution.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 1 smoke scripts and lock validation to them</name>
  <read_first>.planning/phases/01-foundation-and-offline-delivery/01-VALIDATION.md, infra/compose/compose.yaml, scripts/qa/phase1-smoke.sh, scripts/qa/phase1-offline-check.sh, scripts/qa/phase1-queue-check.sh, scripts/qa/phase1-transport-check.sh</read_first>
  <files>scripts/qa/phase1-smoke.sh, scripts/qa/phase1-offline-check.sh, scripts/qa/phase1-queue-check.sh, scripts/qa/phase1-transport-check.sh, .planning/phases/01-foundation-and-offline-delivery/01-VALIDATION.md</files>
  <acceptance_criteria>`scripts/qa/phase1-smoke.sh` runs `docker compose up --build --wait` against the Phase 1 compose file; `scripts/qa/phase1-offline-check.sh` asserts that no external registry/CDN fetch path is required by the repo-controlled build/runtime and that the vendored-store verification procedure is present; `scripts/qa/phase1-queue-check.sh` verifies a trivial `system` queue job by calling `POST /api/v1/system-jobs/echo`; `scripts/qa/phase1-transport-check.sh` verifies HTTP health/meta plus WebSocket handshake; `01-VALIDATION.md` task map references the created script paths with no remaining `❌ W0` placeholders.</acceptance_criteria>
  <action>Implement the smoke harness under `scripts/qa/` exactly around the validation contract: a general startup script, an offline check, a queue check, and a transport check. The queue check must produce work through the deterministic API enqueue path from Plan 03, not by mutating Redis directly or inventing a second queue contract. Update `01-VALIDATION.md` so its placeholders are replaced with the real script/file paths, every Phase 1 task is mapped to the correct plan/task ID, requirements, and threat refs, and the validation document no longer relies on Wave 0 gaps. Keep the checks Phase 1-specific: they should validate startup, health, handshake, queue bootstrap, and vendored dependency integrity procedure only, not application-domain behaviors.</action>
  <verify>
    <automated>test -x scripts/qa/phase1-smoke.sh && test -x scripts/qa/phase1-offline-check.sh && test -x scripts/qa/phase1-queue-check.sh && test -x scripts/qa/phase1-transport-check.sh && ! rg -n "❌ W0" .planning/phases/01-foundation-and-offline-delivery/01-VALIDATION.md && rg -n "01-01-01|01-01-02|01-02-01|01-02-02|01-03-01|01-03-02|01-04-01|01-04-02|T-01-10|T-01-11|T-01-14|system-jobs/echo" scripts/qa/phase1-queue-check.sh .planning/phases/01-foundation-and-offline-delivery/01-VALIDATION.md</automated>
  </verify>
  <done>QA has executable scripts for all four foundation checks, and the phase validation doc points to real files instead of placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker daemon -> service containers | Compose orchestrates startup and readiness for dependent services |
| app containers -> Postgres/Redis | Backend and worker depend on healthy storage and queue services |
| container filesystems -> host volumes | Writable paths can expand attack and mutation surface if left open unnecessarily |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | D | `infra/compose/compose.yaml` | mitigate | Use healthchecks plus `depends_on.condition=service_healthy` so startup race due to unhealthy service dependencies is blocked. |
| T-01-11 | I/D | Dockerfiles and Compose build policy | mitigate | Set `pull_policy: never` where appropriate and force offline install paths from `vendor/pnpm-store` to block hidden runtime internet dependency. |
| T-01-12 | T/E | container filesystem mounts | mitigate | Make service filesystems read-only by default and allow only narrowly scoped writable mounts, addressing unnecessarily writable container filesystems. |
| T-01-14 | T/I | vendored offline dependency delivery path | mitigate | Require the offline smoke and documentation path to verify that Dockerfiles consume the lockfile-backed `vendor/pnpm-store` workflow instead of silently drifting to unverified dependency inputs. |
</threat_model>

<verification>
Run the grep checks above, then use the created scripts as the canonical Phase 1 acceptance loop: startup, offline, queue, and transport.
</verification>

<success_criteria>
Phase 1 can be verified with Compose and smoke scripts that prove offline startup, service health ordering, queue bootstrap, and hybrid transport availability.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-offline-delivery/01-foundation-and-offline-delivery-04-SUMMARY.md`
</output>
