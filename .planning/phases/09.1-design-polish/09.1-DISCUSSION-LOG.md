# Phase 9.1: Design Polish - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-25
**Phase:** 09.1-design-polish
**Areas discussed:** Register inline validation, Frozen DM redesign, Ghost contact handling, Attachment card rendering, Friend request UI, Design requirements audit

---

## 1. Register inline validation — backend API

| Option | Description | Selected |
|--------|-------------|----------|
| Realtime debounce | Check email/username availability on every keystroke with debounce | |
| Submit-only validation | Check availability only on form submit, show inline errors after | ✓ |
| Dedicated check endpoints | Add GET `/auth/check-email` and `/auth/check-username` endpoints | |

**User's choice:** Submit-only validation — no realtime keystroke checking.
**Notes:** Backend already checks uniqueness during registration. Frontend needs to parse error response into field-specific inline feedback (AVAILABLE/TAKEN badges) instead of generic error banner.

---

## 2. Frozen DM — banner and UX

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal badge | Keep current `read-only` badge in header | |
| Full redesign | Full "CONVERSATION FROZEN" banner + redesigned DM header with action buttons | ✓ |

**User's choice:** Full redesign of DM header matching `personal.jsx` design.
**Notes:** Includes frozen banner with ban date, "Unban to resume" link, disabled composer with explanation text, and header buttons (View profile / Unban user when frozen, View profile / Ban user when active).

---

## 3. Ghost contact (deleted user re-register)

| Option | Description | Selected |
|--------|-------------|----------|
| Clean up friendships | Delete friendship rows, remove from contact list entirely | |
| Ghost display + freeze | Freeze DM conversations, show "[deleted]" in contacts, block username re-use | ✓ |
| Allow username re-use | Let new users register with previously-used usernames | |

**User's choice:** Freeze DM (like ban but different reason/text), show "[deleted]" in contacts, block username re-use permanently.
**Notes:** 
- Backend `deleteAccount()` cascade needs to freeze DMs before deleting friendships (currently deletes both)
- Need `deleted_usernames` table to prevent re-registration with used usernames
- "ACCOUNT DELETED" banner in frozen DM — no unban button, different text from ban freeze
- Contact sidebar shows "[deleted]" with offline presence

---

## 4. Attachment card rendering + timeline dividers

No options presented — user confirmed the plan to match design.

**User's choice:** Restyle attachment rendering to card format (icon + filename + size + Download button) and verify divider styling matches design.
**Notes:** Current implementation uses plain links. Design shows structured card with file type icon (DOC/IMG/etc.).

---

## 5. Friend request UI

| Option | Description | Selected |
|--------|-------------|----------|
| Right panel form | Permanent form in right column of contacts page | |
| Redesigned modal | Keep modal approach but restyle to match design Variant B | ✓ |
| New MemberPopover | Popover on room member click per design Variant C | ✓ |

**User's choice:** Restyle existing AddContactModal to match design + new MemberPopover component for room member list.
**Notes:** AddContactModal already has correct logic, needs visual alignment with design. MemberPopover is entirely new — shows avatar, name, presence, context, and action menu (Send friend request / View profile / Ban user).

---

## 6. Design requirements audit

No options presented — designated as a research/planning task.

**User's choice:** Systematic walk-through of Appendix checklist during research/planning phase to identify any additional gaps.
**Notes:** Covers all flows referenced in `index.html` lines 118-134.

---

## Claude's Discretion

- File type icon mapping for attachment cards
- CSS class naming for new components
- Whether frozen DM banners (banned vs deleted) share a component with variant prop

## Deferred Ideas

- Realtime keystroke validation for registration fields
- User profile view/panel
- Message hover action toolbar (icon-based reply/edit/delete)
