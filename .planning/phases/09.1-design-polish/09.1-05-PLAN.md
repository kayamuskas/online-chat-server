---
phase: 09.1-design-polish
plan: 05
type: execute
wave: 2
depends_on: [09.1-02, 09.1-04]
files_modified:
  - apps/web/src/features/contacts/AddContactModal.tsx
  - apps/web/src/features/contacts/MemberPopover.tsx
  - apps/web/src/features/rooms/RoomMembersTable.tsx
  - apps/web/src/styles.css
autonomous: true
requirements:
  - D-9.1-19
  - D-9.1-20
  - D-9.1-21

must_haves:
  truths:
    - "AddContactModal shows monospace title 'ADD FRIEND BY USERNAME', @ prefix on username field, accent-colored Send request button, and footer text"
    - "MemberPopover displays avatar circle, name, @handle with presence dot, context line, and action menu"
    - "Clicking a member row in RoomMembersTable opens MemberPopover instead of showing inline Add friend button"
    - "MemberPopover dismisses on outside click"
  artifacts:
    - path: "apps/web/src/features/contacts/AddContactModal.tsx"
      provides: "Restyled add-friend modal matching design Variant B"
      contains: "ADD FRIEND BY USERNAME"
    - path: "apps/web/src/features/contacts/MemberPopover.tsx"
      provides: "New MemberPopover component for room member interactions"
      contains: "member-popover"
    - path: "apps/web/src/features/rooms/RoomMembersTable.tsx"
      provides: "Wired MemberPopover on member row click"
      contains: "MemberPopover"
  key_links:
    - from: "apps/web/src/features/rooms/RoomMembersTable.tsx"
      to: "apps/web/src/features/contacts/MemberPopover.tsx"
      via: "MemberPopover rendered on member row click"
      pattern: "import.*MemberPopover"
---

<objective>
Friend request UI: AddContactModal restyle and MemberPopover component

Purpose: Restyle AddContactModal to match the design's Variant B (monospace title, @ prefix, accent button, footer). Create a new MemberPopover component for room member rows that replaces the inline "Add friend" button with a positioned popover showing avatar, name, presence, context, and action menu.

Output: Restyled AddContactModal, new MemberPopover component, wired into RoomMembersTable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09.1-design-polish/09.1-CONTEXT.md
@.planning/phases/09.1-design-polish/09.1-RESEARCH.md

<interfaces>
<!-- Current AddContactModal structure -->
<div className="modal-overlay">
  <div className="modal">
    <h3>Add contact</h3>
    <!-- fields: username (required), message (optional) -->
    <!-- buttons: Cancel + Send request -->
  </div>
</div>

<!-- Current RoomMembersTable props -->
interface RoomMembersTableProps {
  members: MemberRow[];
  currentUserId: string;
  ownerUserId: string;
  currentUserIsAdmin: boolean;
  currentUserIsOwner: boolean;
  onMakeAdmin?: (userId: string) => void;
  onRemoveAdmin?: (userId: string) => void;
  onRemoveMember?: (userId: string) => void;
  actionBusy?: string | null;
  onSendFriendRequest?: (userId: string, username: string) => void;
  friendUserIds?: Set<string>;
}

<!-- MemberRow interface -->
export interface MemberRow {
  userId: string;
  username: string;
  membership: RoomMembership;
  presenceStatus?: PresenceStatus;
}

<!-- Design reference: contacts.jsx MemberPopover (lines 133-151) -->
Fixed width: 260px, dark border (var(--ink)), strong shadow
Header: avatar circle (40x40) + name + @handle with presence dot
Context line: "Member of #roomname. Not in your contacts."
Action menu: Send friend request / View profile / Ban user (red)

<!-- Design reference: contacts.jsx Variant B modal (lines 163-175) -->
Title: "ADD FRIEND BY USERNAME" monospace
Field labels: "Username" (with @ prefix), "Message (optional)"
Button: accent-colored "Send request"
Footer: "Requires confirmation from [name]."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AddContactModal to match Variant B design</name>
  <read_first>
    - apps/web/src/features/contacts/AddContactModal.tsx
    - requirements/desing_v1/components/contacts.jsx
    - requirements/desing_v1/styles.css
    - apps/web/src/styles.css
  </read_first>
  <files>apps/web/src/features/contacts/AddContactModal.tsx, apps/web/src/styles.css</files>
  <action>
1. In `AddContactModal.tsx`, update the modal title from `<h3>Add contact</h3>` to:
```tsx
<h3 className="modal__title--mono">ADD FRIEND BY USERNAME</h3>
```

2. Update the username input to show the @ prefix. Wrap the input in a container:
```tsx
<div className="field">
  <label className="field__label" htmlFor="contact-username">Username</label>
  <div className="field__input-group">
    <span className="field__prefix">@</span>
    <input
      id="contact-username"
      className="field__input field__input--prefixed"
      type="text"
      placeholder="username"
      value={username}
      onChange={(e) => setUsername(e.target.value)}
      required
      autoFocus
      autoComplete="off"
    />
  </div>
</div>
```

3. Change the submit button to use accent styling:
```tsx
<button type="submit" className="btn btn--accent" disabled={submitting || !username.trim()}>
  {submitting ? "Sending…" : "Send request"}
</button>
```

4. Add footer text after the form-actions div, inside the form:
```tsx
{username.trim() && (
  <p className="modal__footer-note">
    Requires confirmation from {username.trim()}.
  </p>
)}
```

5. Add CSS to `styles.css`:
```css
/* ── AddContactModal Variant B (D-9.1-19) ─────────────────────── */
.modal__title--mono {
  font-family: var(--mono);
  font-size: 13px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--ink, #333);
  margin-bottom: 12px;
}
.field__input-group {
  display: flex;
  align-items: center;
  border: 1px solid var(--line, #e0e0e0);
  border-radius: 4px;
  overflow: hidden;
}
.field__prefix {
  padding: 6px 0 6px 10px;
  font-family: var(--mono);
  color: var(--muted, #888);
  font-size: 13px;
  user-select: none;
}
.field__input--prefixed {
  border: none;
  padding-left: 4px;
  flex: 1;
}
.field__input--prefixed:focus {
  outline: none;
  box-shadow: none;
}
.field__input-group:focus-within {
  border-color: var(--accent, #6366f1);
  box-shadow: 0 0 0 2px rgba(99, 102, 241, 0.15);
}
.btn--accent {
  background: var(--accent, #6366f1);
  color: #fff;
  border-color: var(--accent, #6366f1);
}
.btn--accent:hover {
  opacity: 0.9;
}
.modal__footer-note {
  font-size: 11px;
  font-family: var(--mono);
  color: var(--muted, #888);
  margin-top: 8px;
  text-align: center;
}
```
  </action>
  <verify>
    <automated>cd /Users/kayama/work/ai/da/hackaton/online-chat-server && grep -q "ADD FRIEND BY USERNAME" apps/web/src/features/contacts/AddContactModal.tsx && grep -q "field__prefix" apps/web/src/features/contacts/AddContactModal.tsx && grep -q "btn--accent" apps/web/src/features/contacts/AddContactModal.tsx && grep -q "Requires confirmation from" apps/web/src/features/contacts/AddContactModal.tsx && grep -q ".modal__title--mono" apps/web/src/styles.css && grep -q ".btn--accent" apps/web/src/styles.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - AddContactModal.tsx title is "ADD FRIEND BY USERNAME" with className "modal__title--mono"
    - AddContactModal.tsx username field has @ prefix via .field__prefix span
    - AddContactModal.tsx submit button uses className "btn btn--accent"
    - AddContactModal.tsx shows "Requires confirmation from [name]." footer when username is entered
    - styles.css contains .modal__title--mono with font-family: var(--mono) and letter-spacing
    - styles.css contains .field__prefix and .field__input--prefixed classes
    - styles.css contains .btn--accent with background: var(--accent)
    - styles.css contains .modal__footer-note
  </acceptance_criteria>
  <done>AddContactModal restyled to Variant B: monospace title, @ prefix, accent button, confirmation footer.</done>
</task>

<task type="auto">
  <name>Task 2: Create MemberPopover and wire into RoomMembersTable</name>
  <read_first>
    - apps/web/src/features/rooms/RoomMembersTable.tsx
    - requirements/desing_v1/components/contacts.jsx
    - requirements/desing_v1/styles.css
    - apps/web/src/styles.css
    - apps/web/src/features/presence/PresenceDot.tsx
  </read_first>
  <files>apps/web/src/features/contacts/MemberPopover.tsx, apps/web/src/features/rooms/RoomMembersTable.tsx, apps/web/src/styles.css</files>
  <action>
1. Create `apps/web/src/features/contacts/MemberPopover.tsx`:

```typescript
import { useEffect, useRef } from "react";
import { PresenceDot, type PresenceStatus } from "../presence/PresenceDot";

interface MemberPopoverProps {
  username: string;
  userId: string;
  presenceStatus: PresenceStatus;
  roomName: string;
  isFriend: boolean;
  isCurrentUser: boolean;
  anchorRect: DOMRect;
  onClose: () => void;
  onSendFriendRequest?: (userId: string, username: string) => void;
  onBanUser?: (userId: string) => void;
}

export function MemberPopover({
  username,
  userId,
  presenceStatus,
  roomName,
  isFriend,
  isCurrentUser,
  anchorRect,
  onClose,
  onSendFriendRequest,
  onBanUser,
}: MemberPopoverProps) {
  const popoverRef = useRef<HTMLDivElement>(null);

  // Outside click dismiss
  useEffect(() => {
    function handleMouseDown(e: MouseEvent) {
      if (popoverRef.current && !popoverRef.current.contains(e.target as Node)) {
        onClose();
      }
    }
    document.addEventListener("mousedown", handleMouseDown);
    return () => document.removeEventListener("mousedown", handleMouseDown);
  }, [onClose]);

  // Position: below the anchor, flip up if near viewport bottom
  const top = anchorRect.bottom + 4;
  const left = anchorRect.left;
  const flipUp = top + 200 > window.innerHeight;
  const style: React.CSSProperties = {
    position: "fixed",
    left: Math.min(left, window.innerWidth - 270),
    top: flipUp ? anchorRect.top - 200 : top,
    zIndex: 1000,
  };

  const initial = username.charAt(0).toUpperCase();
  const contextParts: string[] = [];
  contextParts.push(`Member of #${roomName}`);
  if (isFriend) {
    contextParts.push("In your contacts");
  } else {
    contextParts.push("Not in your contacts");
  }

  return (
    <div ref={popoverRef} className="member-popover" style={style}>
      <div className="member-popover__header">
        <div className="member-popover__avatar">{initial}</div>
        <div className="member-popover__identity">
          <div className="member-popover__name">{username}</div>
          <div className="member-popover__handle">
            <PresenceDot status={presenceStatus} />
            <span>@{username}</span>
          </div>
        </div>
      </div>
      <div className="member-popover__context">
        {contextParts.join(". ")}.
      </div>
      {!isCurrentUser && (
        <div className="member-popover__actions">
          {!isFriend && onSendFriendRequest && (
            <button
              type="button"
              className="member-popover__action"
              onClick={() => { onSendFriendRequest(userId, username); onClose(); }}
            >
              Send friend request
            </button>
          )}
          <button type="button" className="member-popover__action" disabled>
            View profile
          </button>
          {onBanUser && (
            <button
              type="button"
              className="member-popover__action member-popover__action--danger"
              onClick={() => { onBanUser(userId); onClose(); }}
            >
              Ban user
            </button>
          )}
        </div>
      )}
    </div>
  );
}
```

2. In `RoomMembersTable.tsx`:

a. Add imports at top:
```typescript
import { useState, useRef } from "react";
import { MemberPopover } from "../contacts/MemberPopover";
```

b. Add new prop to `RoomMembersTableProps`:
```typescript
/** Room name for popover context line. */
roomName?: string;
/** Called when user clicks Ban in popover on a non-member (user ban). */
onBanUser?: (userId: string) => void;
```

c. Add state inside the component:
```typescript
const [popoverTarget, setPopoverTarget] = useState<{ userId: string; rect: DOMRect } | null>(null);
```

d. Make the member username cell clickable (except for current user). Replace the `<span>` containing `<PresenceDot>` and username with:
```tsx
<span
  style={{ display: "flex", alignItems: "center", gap: "0.5rem", cursor: isCurrentUser ? "default" : "pointer" }}
  onClick={isCurrentUser ? undefined : (e) => {
    const rect = (e.currentTarget as HTMLElement).getBoundingClientRect();
    setPopoverTarget({ userId: m.userId, rect });
  }}
>
```

e. Remove the inline "Add friend" button (the `{!friendUserIds?.has(m.userId) && onSendFriendRequest && ...}` block at lines 139-148). The MemberPopover replaces this (D-9.1-21).

f. Render the popover at the end of the component, before the closing `</table>` or after it:
```tsx
{popoverTarget && (() => {
  const member = members.find(m => m.userId === popoverTarget.userId);
  if (!member) return null;
  return (
    <MemberPopover
      username={member.username}
      userId={member.userId}
      presenceStatus={member.presenceStatus ?? "offline"}
      roomName={roomName ?? ""}
      isFriend={friendUserIds?.has(member.userId) ?? false}
      isCurrentUser={member.userId === currentUserId}
      anchorRect={popoverTarget.rect}
      onClose={() => setPopoverTarget(null)}
      onSendFriendRequest={onSendFriendRequest}
      onBanUser={onBanUser}
    />
  );
})()}
```

3. Add MemberPopover CSS to `styles.css`:
```css
/* ── MemberPopover (D-9.1-20) ─────────────────────────────────── */
.member-popover {
  width: 260px;
  background: var(--bg, #fff);
  border: 1px solid var(--ink, #333);
  border-radius: 6px;
  box-shadow: 0 4px 16px rgba(0,0,0,0.15);
  padding: 14px;
  font-size: 13px;
}
.member-popover__header {
  display: flex;
  gap: 10px;
  align-items: center;
  margin-bottom: 10px;
}
.member-popover__avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--stripe, #eee);
  border: 1px solid var(--line, #ddd);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--mono);
  font-size: 16px;
  font-weight: 600;
  color: var(--ink, #333);
  flex-shrink: 0;
}
.member-popover__identity {
  min-width: 0;
}
.member-popover__name {
  font-weight: 600;
  font-size: 14px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.member-popover__handle {
  display: flex;
  align-items: center;
  gap: 4px;
  font-family: var(--mono);
  font-size: 12px;
  color: var(--muted, #888);
}
.member-popover__context {
  font-size: 11.5px;
  color: var(--muted-2, #999);
  font-family: var(--mono);
  padding: 8px 0;
  border-top: 1px solid var(--line, #eee);
  border-bottom: 1px solid var(--line, #eee);
  margin-bottom: 8px;
}
.member-popover__actions {
  display: flex;
  flex-direction: column;
  gap: 2px;
}
.member-popover__action {
  background: none;
  border: none;
  text-align: left;
  padding: 6px 8px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 13px;
  color: var(--ink, #333);
}
.member-popover__action:hover {
  background: var(--stripe, #f5f5f5);
}
.member-popover__action:disabled {
  color: var(--muted, #aaa);
  cursor: default;
}
.member-popover__action--danger {
  color: var(--danger, #ef4444);
}
.member-popover__action--danger:hover {
  background: #fef2f2;
}
```
  </action>
  <verify>
    <automated>cd /Users/kayama/work/ai/da/hackaton/online-chat-server && test -f apps/web/src/features/contacts/MemberPopover.tsx && grep -q "member-popover" apps/web/src/features/contacts/MemberPopover.tsx && grep -q "MemberPopover" apps/web/src/features/rooms/RoomMembersTable.tsx && grep -q "popoverTarget" apps/web/src/features/rooms/RoomMembersTable.tsx && grep -q ".member-popover" apps/web/src/styles.css && grep -q "member-popover__avatar" apps/web/src/styles.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - MemberPopover.tsx exists with props: username, userId, presenceStatus, roomName, isFriend, isCurrentUser, anchorRect, onClose, onSendFriendRequest, onBanUser
    - MemberPopover.tsx renders avatar circle (40x40) with initial, name, @handle with PresenceDot
    - MemberPopover.tsx renders context line "Member of #roomname. [Not] in your contacts."
    - MemberPopover.tsx renders action menu: "Send friend request" (if not friend), "View profile" (disabled), "Ban user" (red)
    - MemberPopover.tsx has outside-click dismiss via mousedown event listener
    - RoomMembersTable.tsx imports and renders MemberPopover
    - RoomMembersTable.tsx has popoverTarget state that opens on member username click
    - RoomMembersTable.tsx no longer has the inline "Add friend" button (D-9.1-21)
    - styles.css contains .member-popover with width: 260px
    - styles.css contains .member-popover__avatar with width: 40px, height: 40px, border-radius: 50%
    - styles.css contains .member-popover__action--danger with red color
  </acceptance_criteria>
  <done>MemberPopover component created with avatar, identity, context, and action menu. RoomMembersTable wires popover on member row click, replacing inline Add friend button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MemberPopover actions | Friend request and ban actions call existing API endpoints with server-side validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9.1-06 | Tampering | MemberPopover | accept | Actions are UI triggers for existing validated API calls; no new trust boundary |
</threat_model>

<verification>
- Frontend compiles: `cd apps/web && pnpm exec tsc --noEmit`
- Visual check: AddContactModal shows redesigned layout, MemberPopover appears on member click
</verification>

<success_criteria>
- AddContactModal matches Variant B design
- MemberPopover shows on member row click with full action menu
- Inline "Add friend" button removed from RoomMembersTable
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-design-polish/09.1-05-SUMMARY.md`
</output>
