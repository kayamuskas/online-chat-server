---
phase: 05-contacts-and-dm-policy
plan: "06"
type: execute
wave: 6
depends_on:
  - "05-05"
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/features/contacts/ContactsSidebar.tsx
  - apps/web/src/features/contacts/FriendRequestDropdown.tsx
autonomous: false
gap_closure: true
requirements:
  - FRND-03
  - FRND-04
must_haves:
  truths:
    - "An authenticated user can find the contacts management page from the visible shell without editing the URL or guessing a hidden tab."
    - "The contacts management page visibly exposes the existing Remove, Block, and Unblock actions tied to the friend and blocked-user lists."
    - "Blocking a friend still routes the user into the existing Blocked Users list with an obvious unban path."
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "First-class shell navigation into the existing contacts management tab"
    - path: "apps/web/src/features/contacts/ContactsSidebar.tsx"
      provides: "Sidebar-level discoverability affordance for the contacts management page"
    - path: "apps/web/src/features/contacts/FriendRequestDropdown.tsx"
      provides: "Secondary affordance from the requests dropdown into full contacts management"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/features/contacts/ContactsView.tsx"
      via: "explicit user-facing navigation control that sets tab='contacts'"
      pattern: "setTab\\(\"contacts\"\\)|tab === \"contacts\""
    - from: "apps/web/src/features/contacts/ContactsSidebar.tsx"
      to: "apps/web/src/App.tsx"
      via: "callback or button that opens the full contacts management page"
      pattern: "onOpenContacts|Manage contacts|Contacts"
    - from: "apps/web/src/features/contacts/FriendRequestDropdown.tsx"
      to: "apps/web/src/App.tsx"
      via: "dropdown footer/action that opens full contacts management"
      pattern: "onOpenContacts|Manage contacts"
---

<objective>
Close the Phase 5 UAT gap by making the already-built contacts management surface reachable and obvious from the authenticated shell.

Purpose: the current Remove / Block / Unblock UI already exists in `ContactsView`, but users cannot reliably discover it from the visible shell, so FRND-03 and FRND-04 appear missing in UAT.
Output: one focused frontend reachability fix plus a shell-level verification gate that proves the management page and its actions are visible to a real user.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-contacts-and-dm-policy/05-CONTEXT.md
@.planning/phases/05-contacts-and-dm-policy/05-RESEARCH.md
@.planning/phases/05-contacts-and-dm-policy/05-UAT.md
@.planning/phases/05-contacts-and-dm-policy/05-04-SUMMARY.md
@.planning/phases/05-contacts-and-dm-policy/05-05-SUMMARY.md
@apps/web/src/App.tsx
@apps/web/src/features/contacts/ContactsView.tsx
@apps/web/src/features/contacts/ContactsSidebar.tsx
@apps/web/src/features/contacts/FriendRequestDropdown.tsx

<interfaces>
Existing implementation to reuse, not rebuild:

- `apps/web/src/App.tsx` already contains `tab === "contacts" ? <ContactsView ... />` and loads contacts + pending requests.
- `apps/web/src/features/contacts/ContactsView.tsx` already renders:
  - `Incoming Requests`
  - `My Friends` with `Remove` and `Block`
  - `Blocked Users` with `Unblock`
- `apps/web/src/features/contacts/ContactsSidebar.tsx` currently exposes per-contact `Msg` plus `+ Add contact`, but no explicit route into full management.
- `apps/web/src/features/contacts/FriendRequestDropdown.tsx` currently exposes accept/decline only, with no handoff into the full contacts page.

This gap-closure plan must preserve the existing backend and `ContactsView` behavior from 05-01 through 05-05. Do not add new domain logic, API endpoints, or Phase 6 messaging work.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit contacts-management entry points to the authenticated shell</name>
  <read_first>.planning/phases/05-contacts-and-dm-policy/05-UAT.md, .planning/phases/05-contacts-and-dm-policy/05-05-SUMMARY.md, apps/web/src/App.tsx, apps/web/src/features/contacts/ContactsView.tsx, apps/web/src/features/contacts/ContactsSidebar.tsx, apps/web/src/features/contacts/FriendRequestDropdown.tsx</read_first>
  <files>apps/web/src/App.tsx, apps/web/src/features/contacts/ContactsSidebar.tsx, apps/web/src/features/contacts/FriendRequestDropdown.tsx</files>
  <action>Use the existing `ContactsView` as the destination and make it discoverable from surfaces the user already sees immediately after sign-in. In `App.tsx`, add a clear, stable navigation affordance that opens `tab === "contacts"` without requiring URL edits, hidden state knowledge, or the user to click a DM-only row first. Reuse the current contacts loading and pending-request state instead of adding new fetch paths. In `ContactsSidebar.tsx`, add an explicit management CTA distinct from `+ Add contact` and per-row `Msg`, because those actions do not communicate friend removal or blocking management; wire it through a dedicated callback rather than overloading DM behavior. In `FriendRequestDropdown.tsx`, add a secondary `Manage contacts` handoff so a user who arrives through the top-nav badge can reach the full page where Remove / Block / Unblock live. Keep this strictly frontend and discoverability-focused: no backend changes, no new routes, no Phase 6 DM behavior, and no duplicate management UI outside the existing `ContactsView`.</action>
  <verify>
    <automated>pnpm --filter @chat/web build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Manage contacts|Contacts' apps/web/src/features/contacts/ContactsSidebar.tsx apps/web/src/features/contacts/FriendRequestDropdown.tsx apps/web/src/App.tsx` shows a user-facing navigation label for the full contacts page.
    - `rg -n 'setTab\\("contacts"\\)|tab === "contacts"' apps/web/src/App.tsx` shows the visible affordance landing in the existing contacts tab instead of creating a second page.
    - `pnpm --filter @chat/web build` passes.
    - The plan does not modify backend files or replan domain work from 05-01 through 05-05.
  </acceptance_criteria>
  <done>The authenticated shell has an obvious way to open the already-implemented contacts management view, and the user no longer has to discover removal/blocking through hidden tab state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the contacts management page is visibly reachable and exposes friend/block controls</name>
  <read_first>.planning/phases/05-contacts-and-dm-policy/05-UAT.md, apps/web/src/App.tsx, apps/web/src/features/contacts/ContactsView.tsx</read_first>
  <what_built>The shell now exposes a first-class path into the existing `ContactsView`, plus a secondary handoff from the friend-request dropdown.</what_built>
  <how_to_verify>
    1. Start the app normally and sign in through the same shell used during UAT.
    2. From the default authenticated shell, locate the explicit contacts-management entry point without editing the URL or using devtools.
    3. Open it and confirm the page visibly shows the sections `Incoming Requests`, `My Friends`, and `Blocked Users`.
    4. Confirm at least one friend row exposes both `Remove` and `Block` buttons directly on the page.
    5. Trigger `Block` for a friend and verify that the user disappears from `My Friends` and appears in `Blocked Users` with an `Unblock` action.
    6. Confirm the friend-request bell/dropdown also exposes a visible handoff into the same management page, so the management surface remains discoverable from the top navigation too.
  </how_to_verify>
  <resume_signal>Type `approved` if the shell-level check passed, or reply with the missing/unclear entry point.</resume_signal>
  <acceptance_criteria>The user-visible shell makes the contacts management page easy to find, and the same page clearly exposes Remove / Block / Unblock so the original UAT complaint cannot recur because the feature is merely hidden.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> frontend shell navigation | User-controlled clicks choose which tab to open, but must not bypass backend policy or invent a second management surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | T | `apps/web/src/App.tsx` contacts navigation | mitigate | Reuse the existing `tab === "contacts"` branch and existing `ContactsView`; do not fork separate management state that could drift from the real implementation |
| T-05-06-02 | I | contacts-management discoverability controls | accept | The added affordances reveal only an already-authenticated in-app page; no new data source or cross-user information is introduced |
| T-05-06-03 | E | frontend-only shell change | mitigate | Keep all remove/block/unblock enforcement in the existing backend APIs; the new shell controls must only navigate to current UI, not change authorization behavior |
</threat_model>

<verification>
- [ ] `pnpm --filter @chat/web build`
- [ ] `rg -n 'Manage contacts|Contacts' apps/web/src/features/contacts/ContactsSidebar.tsx apps/web/src/features/contacts/FriendRequestDropdown.tsx apps/web/src/App.tsx`
- [ ] Human shell verification proves the page is reachable without URL editing and visibly shows `Remove`, `Block`, and `Unblock`
</verification>

<success_criteria>
- The only addressed gap is the UAT failure that the contacts management page and its controls are not reachable/visible.
- Existing `ContactsView` implementation is reused rather than replaced or duplicated.
- The authenticated shell contains an obvious path to the full contacts page from at least one persistent shell surface, with a secondary path from the requests dropdown.
- A human can reproduce the original UAT scenario and now find the page and buttons from the visible shell.
</success_criteria>

<output>
After completion, create `.planning/phases/05-contacts-and-dm-policy/05-06-SUMMARY.md`
</output>
