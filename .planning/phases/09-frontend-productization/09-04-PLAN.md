---
phase: 09-frontend-productization
plan: 04
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - apps/web/src/features/messages/MessageTimeline.tsx
  - apps/web/src/styles.css
autonomous: true
requirements:
  - MSG-07
must_haves:
  truths:
    - "Older history loads when the user scrolls to the top of the timeline."
    - "Prepending older messages preserves viewport position instead of jumping the user to a different part of the thread."
    - "Existing smart autoscroll for new realtime messages remains intact."
  artifacts:
    - path: "apps/web/src/features/messages/MessageTimeline.tsx"
      provides: "Infinite upward history loading behavior"
    - path: "apps/web/src/styles.css"
      provides: "Timeline load-state and new-message affordance styling"
---

<objective>
Ship the upward infinite-scroll behavior deferred into Phase 9.

Purpose: replace the manual older-history affordance with a product-grade
timeline interaction that respects the existing watermark/range contract.

Output: timeline auto-loads older history near the top edge, preserves scroll
position across prepends, and keeps realtime scroll behavior coherent.
</objective>
