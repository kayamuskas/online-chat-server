---
phase: 09-frontend-productization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/styles.css
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "The authenticated frontend shell becomes a real classic chat layout instead of a transitional implementation screen."
    - "The layout has a stable left navigation rail, center conversation area, and right context/member rail."
    - "When a room or DM conversation is active, room navigation compacts instead of keeping the current loose transitional arrangement."
    - "The refactor preserves existing room, contact, DM, and settings entry points instead of regressing completed flows."
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "Phase 9 product shell and top-level layout orchestration"
    - path: "apps/web/src/styles.css"
      provides: "Classic chat shell layout, spacing, and responsive rules"
  key_links:
    - from: "left navigation rail"
      to: "active room/dm/session/account surface"
      via: "top-level shell selection state"
      pattern: "active"
    - from: "active conversation state"
      to: "compact room navigation behavior"
      via: "conditional layout mode"
      pattern: "compact"
---

<objective>
Create the first real product shell for Phase 9.

Purpose: satisfy the structural part of `UI-01` and `UI-02` by replacing the
current transitional authenticated layout with a deliberate classic chat shell.

Output: updated `App.tsx` and `styles.css` that establish the stable three-column
layout and compact-navigation behavior used by all later Phase 9 work.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-frontend-productization/09-CONTEXT.md
@.planning/phases/09-frontend-productization/09-RESEARCH.md
@.planning/phases/05-contacts-and-dm-policy/05-CONTEXT.md
@.planning/phases/06-messaging-core/06-CONTEXT.md
@.planning/phases/04-rooms-and-membership/04-VERIFICATION.md

<interfaces>
Shell contract for this plan:
- left rail: rooms + contacts navigation
- center: active room/DM/settings surface
- right rail: room members or contextual panel when relevant

This plan must preserve existing feature entry points. It is a shell/layout plan,
not an unread/infinite-scroll/admin behavior plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the authenticated shell into the classic three-column layout</name>
  <files>apps/web/src/App.tsx, apps/web/src/styles.css</files>
  <acceptance_criteria>`App.tsx` renders a stable left/center/right shell for authenticated users; the current room/contact/message/settings surfaces remain reachable; CSS defines clear desktop layout plus sensible mobile fallback; the new shell does not regress sign-in/authenticated routing.</acceptance_criteria>
  <action>Refactor the authenticated portion of `App.tsx` so it reads like a product shell rather than a feature pile-up. Introduce explicit shell regions, move existing surface selection into a clearer top-level layout flow, and define the matching CSS structure in `styles.css`. Preserve existing completed features even if their visual embedding changes.</action>
  <verify>
    <automated>rg -n "contacts|rooms|dm|session|password|account|aside|main" apps/web/src/App.tsx && rg -n "grid|sidebar|panel|rail|layout" apps/web/src/styles.css</automated>
  </verify>
  <done>The app has a coherent product shell that later Phase 9 plans can extend without another top-level rewrite.</done>
</task>

<task type="auto">
  <name>Task 2: Add compact navigation mode for active conversations</name>
  <files>apps/web/src/App.tsx, apps/web/src/styles.css</files>
  <acceptance_criteria>When a room or DM is active, the navigation shell shifts into a compact mode consistent with `UI-02`; the compact state remains readable and reversible; no room/contact selection flow breaks.</acceptance_criteria>
  <action>Implement the compact-navigation behavior in the left rail for active conversations. Keep it structurally simple: the user should still be able to navigate rooms and contacts, but the shell should visually prioritize the active conversation and stop looking like a debug/workbench layout.</action>
  <verify>
    <automated>rg -n "compact|active room|active dm|selected" apps/web/src/App.tsx apps/web/src/styles.css</automated>
  </verify>
  <done>The shell now expresses the classic chat navigation behavior required for the rest of Phase 9.</done>
</task>

</tasks>

<success_criteria>
Phase 9 begins with a stable frontend shell that later unread, infinite-scroll,
and admin-UX plans can build on.
</success_criteria>
