---
phase: 09-frontend-productization
plan: 03
type: execute
wave: 1
depends_on:
  - 09-01
  - 09-02
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/features/contacts/ContactsSidebar.tsx
  - apps/web/src/features/messages/DmChatView.tsx
  - apps/web/src/styles.css
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
must_haves:
  truths:
    - "Known room rows and known DM contact rows show visible unread indicators."
    - "Opening a room chat or DM clears the corresponding unread indicator."
    - "Unread behavior stays lightweight and client-owned without inventing a new backend unread system."
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "Unread state orchestration and shell integration"
    - path: "apps/web/src/features/contacts/ContactsSidebar.tsx"
      provides: "Unread rendering on contact rows"
    - path: "apps/web/src/features/messages/DmChatView.tsx"
      provides: "DM conversation identity surfaced back to shell state"
  key_links:
    - from: "socket message-created events"
      to: "room/contact unread counters"
      via: "lightweight client-side unread maps"
      pattern: "increment|clear"
---

<objective>
Implement the first shipped unread-indicator model for Phase 9.

Purpose: satisfy `NOTF-01` and `NOTF-02` without waiting for a heavier durable
read-state backend.

Output: visible unread badges in the navigation shell for known rooms and known
DM contacts, with clear-on-open behavior.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-frontend-productization/09-CONTEXT.md
@apps/web/src/App.tsx
@apps/web/src/features/contacts/ContactsSidebar.tsx
@apps/web/src/features/messages/DmChatView.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add lightweight unread maps to the shell</name>
  <files>apps/web/src/App.tsx</files>
  <acceptance_criteria>The shell tracks unread counts for known room ids and known DM partner ids using client-side state fed by websocket events.</acceptance_criteria>
  <action>Track known room channels and known DM conversations in the shell, subscribe to their socket events, increment unread counts when incoming messages land outside the active view, and clear them when the user opens the corresponding room or DM.</action>
  <verify>
    <automated>rg -n "unread|message-created|joinRoom|joinDm|clear" apps/web/src/App.tsx</automated>
  </verify>
  <done>Unread logic exists in one top-level place instead of being scattered across feature views.</done>
</task>

<task type="auto">
  <name>Task 2: Render unread indicators in room and contact navigation rows</name>
  <files>apps/web/src/App.tsx, apps/web/src/features/contacts/ContactsSidebar.tsx, apps/web/src/styles.css</files>
  <acceptance_criteria>Unread badges appear next to known room names and contact names in the navigation shell, and active rows do not keep stale unread indicators.</acceptance_criteria>
  <action>Extend the left rail so it lists known rooms and contacts in a product-style navigation surface, then render concise unread badges and active-row states with styling that matches the `desing_v1` baseline.</action>
  <verify>
    <automated>rg -n "badge|thread|unread|ContactsSidebar" apps/web/src/App.tsx apps/web/src/features/contacts/ContactsSidebar.tsx apps/web/src/styles.css</automated>
  </verify>
  <done>The classic chat shell now exposes the missing unread navigation affordances required by Phase 9.</done>
</task>
</tasks>
