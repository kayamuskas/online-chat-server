---
phase: 09-frontend-productization
plan: 02
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/styles.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "The shipped shell moves visibly closer to the visual baseline in `requirements/desing_v1/`."
    - "The top navigation, left rail, and right context rail feel like one product language rather than a dark utility dashboard."
    - "Typography hierarchy uses serif headlines/logo and mono metadata labels in a way consistent with the design baseline."
    - "The shell uses a warm light palette derived from the design baseline rather than the current dark developer-facing palette."
  artifacts:
    - path: "apps/web/src/styles.css"
      provides: "Phase 9 visual baseline closer to `requirements/desing_v1/`"
    - path: "apps/web/src/App.tsx"
      provides: "Top navigation and shell structure aligned to design baseline"
  key_links:
    - from: "requirements/desing_v1/styles.css"
      to: "apps/web/src/styles.css"
      via: "palette and typography translation"
      pattern: "serif|mono|accent|warm"
---

<objective>
Bring the Phase 9 shell closer to the agreed design baseline in
`requirements/desing_v1/`.

Purpose: ensure the frontend does not satisfy the structural chat requirements
while drifting away from the already-provided product direction.

Output: refined shell styling and navigation hierarchy aligned to the warm,
classic, wireframe-driven design language.
</objective>

<context>
@requirements/wireframes.md
@requirements/desing_v1/index.html
@requirements/desing_v1/styles.css
@requirements/desing_v1/components/main_chat.jsx
@requirements/desing_v1/components/rooms.jsx
@requirements/desing_v1/components/contacts.jsx
@requirements/desing_v1/components/manage.jsx
@.planning/phases/09-frontend-productization/09-CONTEXT.md
@.planning/phases/09-frontend-productization/09-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Adopt the desing_v1 visual grammar in the shipped shell</name>
  <files>apps/web/src/App.tsx, apps/web/src/styles.css</files>
  <acceptance_criteria>The app shell clearly reflects the warm light palette, serif+mono hierarchy, top-nav emphasis, and classic-chat framing from `requirements/desing_v1/` while staying implementation-real and local-build-safe.</acceptance_criteria>
  <action>Translate the design baseline into the real app shell. Do not copy CDN-dependent implementation details; instead port the visual grammar and interaction hierarchy into the shipped React/Vite code. Favor `desing_v1` over the old dark utility palette wherever there is conflict.</action>
  <verify>
    <automated>rg -n "font-family|serif|mono|accent|app-topbar|app-account|app-shell" apps/web/src/styles.css apps/web/src/App.tsx</automated>
  </verify>
  <done>Phase 9 is now visibly converging toward the promised design baseline instead of only satisfying structural requirements.</done>
</task>
</tasks>
