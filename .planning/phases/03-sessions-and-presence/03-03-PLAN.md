---
phase: 03-sessions-and-presence
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/styles.css
  - apps/web/src/lib/api.ts
  - apps/web/src/features/account/ActiveSessionsView.tsx
  - apps/web/src/features/account/SessionRow.tsx
  - apps/web/src/features/account/RevokeSessionConfirm.tsx
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-07
must_haves:
  truths:
    - "The authenticated web UI shows the Phase 3 active-sessions screen in the style locked by the reference."
    - "The current session is marked as `This browser` and can be revoked from the same UI."
    - "`Sign out all other sessions` is visible, explicit, and separate from per-row revoke."
  artifacts:
    - path: "apps/web/src/features/account/ActiveSessionsView.tsx"
      provides: "Phase 3 active-sessions inventory screen"
    - path: "apps/web/src/lib/api.ts"
      provides: "Client bindings for inventory, revoke, and revoke-all-other-sessions"
    - path: "apps/web/src/features/account/RevokeSessionConfirm.tsx"
      provides: "Reusable confirmation flow for destructive session actions"
  key_links:
    - from: "apps/web/src/features/account/ActiveSessionsView.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "session inventory and revoke calls"
      pattern: "sessions|revoke|other sessions"
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/features/account/ActiveSessionsView.tsx"
      via: "authenticated account route"
      pattern: "/account|sessions"
    - from: "apps/web/src/features/account/SessionRow.tsx"
      to: ".planning/phases/03-sessions-and-presence/03-CONTEXT.md"
      via: "locked table layout and humanized last-active semantics"
      pattern: "This browser|2h ago|yesterday|IP"
---

<objective>
Ship the Phase 3 active-sessions account UI.

Purpose: replace the Phase 2 minimal sign-out card with the session inventory screen the user approved, including current-session marker, humanized timestamps, per-row revoke, and `sign out all other sessions`.
Output: React session-management views, API client glue, and current-session revoke behavior that returns to sign-in immediately.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-sessions-and-presence/03-CONTEXT.md
@.planning/phases/03-sessions-and-presence/03-RESEARCH.md
@requirements/wireframes.md
@requirements/desing_v1/index.html
@apps/web/src/App.tsx
@apps/web/src/styles.css
@apps/web/src/features/account/SessionActionsView.tsx

<interfaces>
The shipped Phase 3 sessions UI must preserve:

- active session list/table layout
- `This browser` marker
- full IP display
- primary `Last active` as humanized relative time
- optional exact timestamp as secondary detail or hover
- per-row `Sign out`
- explicit `Sign out all other sessions`

Do not broaden this plan into rooms, contacts, or full chat-shell feature work.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the authenticated account route with the Phase 3 active-sessions screen</name>
  <read_first>.planning/phases/03-sessions-and-presence/03-CONTEXT.md, .planning/phases/03-sessions-and-presence/03-RESEARCH.md, requirements/wireframes.md, apps/web/src/App.tsx, apps/web/src/styles.css, apps/web/src/features/account/SessionActionsView.tsx</read_first>
  <files>apps/web/src/App.tsx, apps/web/src/styles.css, apps/web/src/lib/api.ts, apps/web/src/features/account/ActiveSessionsView.tsx, apps/web/src/features/account/SessionRow.tsx</files>
  <acceptance_criteria>The authenticated web app shows an active-sessions view closely aligned to the locked reference: `Device / Browser`, `IP`, and `Last active` columns, `This browser` badge, full IP values, per-row sign-out action, and a visible `Sign out all other sessions` action. Humanized last-active labels are primary, with exact timestamp as optional secondary detail.</acceptance_criteria>
  <action>Replace the minimal Phase 2 current-session card with the full Phase 3 active-sessions experience. Reuse the current `/account` entry point rather than introducing unnecessary navigation complexity. Keep the UI close to the reference and make the session table itself the primary account surface for this phase.</action>
  <verify>
    <automated>rg -n "Active sessions|This browser|Last active|Sign out all other sessions|yesterday|ago|IP" apps/web/src/App.tsx apps/web/src/features/account apps/web/src/lib/api.ts</automated>
  </verify>
  <done>The web client can render a real active-session inventory instead of only the current-session sign-out card.</done>
</task>

<task type="auto">
  <name>Task 2: Make destructive session actions explicit and current-session revoke immediate</name>
  <read_first>.planning/phases/03-sessions-and-presence/03-CONTEXT.md, apps/web/src/lib/api.ts, apps/web/src/App.tsx, apps/web/src/features/account/ActiveSessionsView.tsx</read_first>
  <files>apps/web/src/features/account/RevokeSessionConfirm.tsx, apps/web/src/features/account/ActiveSessionsView.tsx, apps/web/src/App.tsx, apps/web/src/lib/api.ts</files>
  <acceptance_criteria>Per-row revoke and `sign out all other sessions` use an explicit confirm interaction; revoking `This browser` immediately returns the current tab to sign-in; revoking other sessions updates the screen quickly without clearing unrelated local state.</acceptance_criteria>
  <action>Add the smallest explicit confirmation UX that still feels fast, then wire current-session revoke to the existing sign-in transition path. Keep action semantics crisp: one destructive action per intent, no ambiguous “logout” wording, no hidden bulk behavior.</action>
  <verify>
    <automated>rg -n "confirm|revoke|current session|This browser|sign in|other sessions" apps/web/src/features/account apps/web/src/App.tsx apps/web/src/lib/api.ts && test -f apps/web/src/features/account/RevokeSessionConfirm.tsx</automated>
  </verify>
  <done>Session revocation feels deliberate but fast, and revoking the current browser behaves exactly as the Phase 3 context requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active-sessions UI -> destructive account actions | Revoke interactions can terminate the user's own current session or other devices |
| web client -> session-management API | UI labeling must not blur per-row revoke vs bulk revoke semantics |
| current-session revoke -> local auth state | The web app must immediately drop local authenticated state when `This browser` is revoked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | T | session-management UI semantics | mitigate | Keep destructive actions explicit, separate, and aligned to the locked reference wording. |
| T-03-09 | D | current-session revoke flow | mitigate | Route current-session revoke through the same sign-in fallback path used by explicit sign-out so auth state clears immediately. |
</threat_model>

<verification>
Run the grep checks above, then exercise the active-sessions screen against the local stack before shared presence-surface work begins.
</verification>

<success_criteria>
The web app ships the approved active-sessions UX and handles current-session revoke, other-session revoke, and bulk-other-session revoke distinctly and safely.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sessions-and-presence/03-sessions-and-presence-03-SUMMARY.md`
</output>

