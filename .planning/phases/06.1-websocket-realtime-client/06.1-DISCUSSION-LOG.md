# Phase 6.1: WebSocket Real-Time Client - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 6.1 - WebSocket Real-Time Client
**Areas discussed:** Socket client architecture, Friend request notifications, Presence updates, Autoscroll, Reconnection

---

## Socket Client Architecture

| Option | Description | Selected |
|--------|-------------|----------|
| React context/provider | Single socket shared app-wide via React context | ✓ |
| Per-view socket | Separate socket per conversation component | |
| Global module singleton | Module-level socket outside React tree | |

**User's choice:** React context/provider
**Notes:** User is not a frontend specialist — delegated implementation details (context shape, hook API) to Claude's Discretion. Provider connects after login, disconnects on sign-out.

---

## Friend Request Notifications

| Option | Description | Selected |
|--------|-------------|----------|
| Polling only | Keep existing REST poll, no backend changes | ✓ |
| Backend push events | Extend AppGateway to emit friend-request-received | |

**User's choice:** Polling only — no backend changes in Phase 6.1.
**Notes:** AppGateway not extended for friend-request events.

---

## Presence Updates

| Option | Description | Selected |
|--------|-------------|----------|
| On-demand poll | Client calls getPresence WS event when needed | ✓ |
| Server-push | AppGateway broadcasts status changes to subscribers | |

**User's choice:** On-demand poll via existing getPresence.

---

## Autoscroll Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Always scroll | Force-scroll to bottom on every new WS message | |
| Smart autoscroll | Only scroll if user is already at bottom | ✓ |

**User's choice:** Smart autoscroll.
**Follow-up:** Show "↓ новые сообщения" button when user is scrolled up and new message arrives. ✓

---

## Reconnection Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Silent reconnect + refetch from last watermark | Reconnect silently and fetch missed messages by watermark | ✓ |
| Silent reconnect + reload last page | Reconnect and reload the most recent history page | |
| Visible reconnecting indicator | Show spinner or banner during reconnect | |

**User's choice:** Silent reconnect + refetch from last watermark.

---

## Claude's Discretion

- Exact socket context shape (React Context, Zustand, module singleton)
- "↓ новые сообщения" button visual style (floating pill, sticky bar, badge)
- Reconnect debounce/throttle to avoid hammering REST on flapping connections

## Deferred Ideas

- Server-push friend request notifications — future phase
- Server-push presence broadcasts — future phase
- Typing indicators — out of scope
- Read receipts — out of scope
