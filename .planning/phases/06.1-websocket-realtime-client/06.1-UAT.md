---
status: partial
phase: 06.1-websocket-realtime-client
source:
  - 06.1-01-SUMMARY.md
  - 06.1-02-SUMMARY.md
  - 06.1-03-SUMMARY.md
  - 06.1-04-SUMMARY.md
  - 06.1-05-SUMMARY.md
started: 2026-04-20T08:47:06Z
updated: 2026-04-20T08:47:06Z
---

## Current Test

[testing paused — 8 items outstanding]

## Tests

### 1. Authenticated socket handshake
expected: After signing in, the realtime socket authenticates with the existing session cookie and responds to socket events instead of silently connecting as an unauthenticated client.
result: issue
reported: "Runtime UAT harness connected to Socket.IO but all authenticated events timed out; `apps/api/src/ws/ws-auth.ts` extracts cookie `session` while HTTP auth issues `chat_session` in `apps/api/src/auth/session-cookie.ts`."
severity: blocker

### 2. Room realtime delivery
expected: When two signed-in users are subscribed to the same room, a message created by one user appears for the other without refresh.
result: issue
reported: "POST /api/v1/messages/rooms/:roomId/messages succeeded, but the subscribed peer never received `message-created` during runtime UAT."
severity: blocker

### 3. Room edit fanout
expected: Editing a room message updates the existing message in-place for other subscribed users without refresh.
result: issue
reported: "PATCH /api/v1/messages/rooms/:roomId/messages/:messageId succeeded, but the subscribed peer never received `message-edited` during runtime UAT."
severity: blocker

### 4. DM realtime delivery
expected: When both participants are subscribed to the same DM conversation, a newly sent message appears for the other participant without refresh.
result: issue
reported: "POST /api/v1/messages/dm/:conversationId/messages succeeded, but the subscribed peer never received `message-created` during runtime UAT."
severity: blocker

### 5. DM edit fanout
expected: Editing a DM message updates the existing message in-place for the other participant without refresh.
result: issue
reported: "PATCH /api/v1/messages/dm/:conversationId/messages/:messageId succeeded, but the subscribed peer never received `message-edited` during runtime UAT."
severity: blocker

### 6. Presence request-response
expected: An authenticated socket can request presence for known users and receives `online` / `offline` updates via the `presence` event.
result: issue
reported: "Authenticated `getPresence` requests timed out during runtime UAT; no `presence` response arrived while the same users were active over HTTP."
severity: blocker

### 7. Smart autoscroll and new-message pill
expected: When the user is reading older history, incoming realtime messages do not yank the viewport and the `↓ новые сообщения` affordance appears until the user returns to bottom.
result: blocked
blocked_by: prior-phase
reason: "Browser-level check is blocked because authenticated realtime delivery is currently failing."

### 8. Reconnect recovery and history catch-up
expected: After a brief disconnect, the active room or DM resubscribes, refetches missed history, and merges any missing messages without losing timeline state.
result: blocked
blocked_by: prior-phase
reason: "Reconnect behavior cannot be meaningfully validated until authenticated websocket delivery is restored."

## Summary

total: 8
passed: 0
issues: 6
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: "Realtime sockets must authenticate with the same session cookie used by HTTP auth."
  status: failed
  reason: "Runtime UAT found a cookie-name mismatch: `apps/api/src/ws/ws-auth.ts` looks for `session`, while `apps/api/src/auth/session-cookie.ts` sets `chat_session`."
  severity: blocker
  test: 1
  root_cause: "WebSocket authentication extracts the wrong cookie name, so authenticated socket handlers silently ignore the client."
  artifacts:
    - path: "apps/api/src/ws/ws-auth.ts"
      issue: "Hard-coded cookie name is `session`."
    - path: "apps/api/src/auth/session-cookie.ts"
      issue: "Canonical HTTP session cookie name is `chat_session`."
  missing:
    - "Share the canonical session cookie name between HTTP auth and websocket auth."
    - "Re-run room, DM, and presence realtime UAT after the cookie-name fix."
  debug_session: ""

- truth: "Room subscriptions must receive realtime create/edit fanout after authenticated join."
  status: failed
  reason: "Room `message-created` and `message-edited` events timed out in runtime UAT."
  severity: blocker
  test: 2
  root_cause: "Downstream effect of unauthenticated websocket sockets caused by the cookie-name mismatch."
  artifacts:
    - path: "apps/api/src/ws/ws-auth.ts"
      issue: "Socket handshake auth fails before `joinRoom` can become effective."
    - path: "apps/api/src/messages/messages.gateway.ts"
      issue: "Join handlers ignore unauthenticated sockets, so room fanout never reaches the client."
  missing:
    - "Fix websocket session extraction."
    - "Re-run room create/edit fanout verification with two authenticated sockets."
  debug_session: ""

- truth: "DM subscriptions must receive realtime create/edit fanout after authenticated join."
  status: failed
  reason: "DM `message-created` and `message-edited` events timed out in runtime UAT."
  severity: blocker
  test: 4
  root_cause: "Downstream effect of unauthenticated websocket sockets caused by the cookie-name mismatch."
  artifacts:
    - path: "apps/api/src/ws/ws-auth.ts"
      issue: "Socket handshake auth fails before `joinDm` can become effective."
    - path: "apps/api/src/messages/messages.gateway.ts"
      issue: "Join handlers ignore unauthenticated sockets, so DM fanout never reaches the client."
  missing:
    - "Fix websocket session extraction."
    - "Re-run DM create/edit fanout verification with two authenticated sockets."
  debug_session: ""

- truth: "Presence polling must return live statuses for authenticated users."
  status: failed
  reason: "Authenticated `getPresence` requests timed out during runtime UAT."
  severity: blocker
  test: 6
  root_cause: "AppGateway never associates the socket with the signed-in user because websocket auth reads the wrong cookie."
  artifacts:
    - path: "apps/api/src/ws/ws-auth.ts"
      issue: "Presence gateway receives no valid session token from the handshake."
    - path: "apps/api/src/ws/app.gateway.ts"
      issue: "Unauthenticated sockets are disconnected or ignored, so `getPresence` yields no response."
  missing:
    - "Fix websocket session extraction."
    - "Re-run presence UAT for online/offline transitions after reconnect."
  debug_session: ""
