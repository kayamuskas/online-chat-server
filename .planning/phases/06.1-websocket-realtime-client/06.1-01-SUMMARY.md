---
phase: 06.1-websocket-realtime-client
plan: "01"
subsystem: websocket-foundation
tags: [websocket, cors, dependency, blocker]
dependency_graph:
  requires: []
  provides: ["gateway-cors-fix", "socket.io-client-installed"]
  affects: ["MessagesGateway", "AppGateway", "web build graph"]
tech_stack:
  added: ["socket.io-client@4.8.3"]
  patterns: ["credentialed websocket cors", "workspace lockfile sync"]
key_files:
  modified:
    - apps/api/src/messages/messages.gateway.ts
    - apps/api/src/ws/app.gateway.ts
    - apps/web/package.json
    - pnpm-lock.yaml
decisions:
  - "Both NestJS gateways now allow only http://localhost:4173 with credentials:true so session-cookie auth works in browsers."
  - "apps/web depends on socket.io-client 4.8.3 to match the backend Socket.IO major version."
metrics:
  duration_seconds: 240
  completed_date: "2026-04-19T19:30:55Z"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 4
---

# Phase 6.1 Plan 01 Summary

**One-liner:** Removed the credentialed WebSocket CORS blocker and added `socket.io-client` to the web workspace.

## What Was Built

Updated `MessagesGateway` and `AppGateway` to use `origin: 'http://localhost:4173'` with `credentials: true` so the browser can send the session cookie during the Socket.IO handshake.

Added `socket.io-client@4.8.3` to `apps/web/package.json` and refreshed `pnpm-lock.yaml` so the frontend can compile against the realtime client library.

## Verification

- [x] `pnpm install --lockfile-only`
- [x] `pnpm install --force`
- [x] `pnpm --filter @chat/api build`
- [x] `pnpm --filter @chat/web build`

## Deviations from Plan

None.
