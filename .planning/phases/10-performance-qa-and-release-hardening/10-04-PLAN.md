---
phase: 10-performance-qa-and-release-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - package.json
  - scripts/qa/release-gate.sh
  - scripts/perf/reduce-artifacts.mjs
  - artifacts/perf/baseline.json
  - artifacts/perf/.gitignore
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - OPS-01
  - OPS-02
  - TEST-01
must_haves:
  truths:
    - "Phase 10 has one blocking release-gate entrypoint that runs startup, perf-lite, p95 latency, and 100k Playwright proof in the required order."
    - "Machine-readable performance evidence persists as baseline, current, and diff JSON per D-1009 and D-1010."
    - "Any regression in startup, perf-lite, p95 latency, or Playwright history proof fails the release gate with actionable output."
  artifacts:
    - path: "scripts/qa/release-gate.sh"
      provides: "Strict end-to-end Phase 10 release gate entrypoint"
    - path: "scripts/perf/reduce-artifacts.mjs"
      provides: "Baseline/current/diff artifact synthesis"
    - path: "artifacts/perf/baseline.json"
      provides: "Committed baseline for regression comparison"
    - path: "artifacts/perf/.gitignore"
      provides: "Policy for keeping current/diff artifacts local while preserving the committed baseline"
    - path: "package.json"
      provides: "Discoverable Phase 10 release/perf scripts"
  key_links:
    - from: "scripts/qa/release-gate.sh"
      to: "scripts/qa/phase10-startup-gate.sh"
      via: "ordered blocking orchestration"
      pattern: "phase10-startup-gate\\.sh"
    - from: "scripts/qa/release-gate.sh"
      to: "scripts/perf/reduce-artifacts.mjs"
      via: "artifact reduction after raw gate outputs"
      pattern: "reduce-artifacts\\.mjs"
    - from: "scripts/perf/reduce-artifacts.mjs"
      to: "artifacts/perf/baseline.json"
      via: "regression diff computation"
      pattern: "baseline\\.json"
---

<objective>
Wire the strict Phase 10 release gate and artifact synthesis.

Purpose: satisfy D-1007, D-1009, and D-1010 by turning the earlier plans into a
single blocking command with durable machine-readable regression evidence.

Output: package scripts, a release-gate shell entrypoint, and an artifact
reducer that persist baseline/current/diff JSON and fail the run on any gate
breach.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-performance-qa-and-release-hardening/10-CONTEXT.md
@.planning/phases/10-performance-qa-and-release-hardening/10-RESEARCH.md
@package.json
@scripts/qa/phase10-startup-gate.sh
@scripts/perf/perf-lite.yml
@scripts/perf/probe-latency.mjs
@scripts/perf/seed-history.mjs
@e2e/perf/history-100k.spec.ts

<interfaces>
Required raw inputs from earlier plans:
```text
artifacts/perf/artillery-current.json
artifacts/perf/latency-current.json
artifacts/perf/history-seed-current.json
artifacts/perf/playwright-critical-current.json
artifacts/perf/playwright-history-current.json
Playwright JSON reporter commands for:
- critical UAT suite:
  PLAYWRIGHT_JSON_OUTPUT_NAME=artifacts/perf/playwright-critical-current.json pnpm exec playwright test e2e/realtime/ws-auth.spec.ts e2e/realtime/room-realtime.spec.ts e2e/realtime/dm-realtime.spec.ts e2e/realtime/presence.spec.ts e2e/realtime/autoscroll.spec.ts e2e/realtime/reconnect.spec.ts e2e/realtime/unread-sidebar.spec.ts e2e/realtime/history-load.spec.ts --reporter=json
- dedicated 100k proof:
  PLAYWRIGHT_JSON_OUTPUT_NAME=artifacts/perf/playwright-history-current.json pnpm exec playwright test e2e/perf/history-100k.spec.ts --reporter=json
Reducer contract:
  node scripts/perf/reduce-artifacts.mjs --baseline artifacts/perf/baseline.json --artillery artifacts/perf/artillery-current.json --latency artifacts/perf/latency-current.json --history-seed artifacts/perf/history-seed-current.json --playwright-critical artifacts/perf/playwright-critical-current.json --playwright-history artifacts/perf/playwright-history-current.json --current artifacts/perf/current.json --diff artifacts/perf/diff.json
```

From package.json:
```json
{
  "scripts": {
    "compose:smoke": "docker compose -f infra/compose/compose.yaml up --build --wait",
    "test:e2e": "playwright test"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synthesize baseline, current, and diff artifacts</name>
  <files>scripts/perf/reduce-artifacts.mjs, artifacts/perf/baseline.json, artifacts/perf/.gitignore</files>
  <action>Implement `reduce-artifacts.mjs` to merge the raw outputs from Artillery, latency probing, history seeding, and both machine-readable Playwright JSON runs into a normalized `current.json`, compare that against a committed `baseline.json`, and emit `diff.json`. The reducer interface must accept explicit CLI input paths for `--artillery`, `--latency`, `--history-seed`, `--playwright-critical`, and `--playwright-history` so `release-gate.sh` can pass deterministic raw artifact locations. Commit a baseline that reflects the intended first accepted Phase 10 gate shape, and use `.gitignore` so future `current.json`, `diff.json`, and raw run files stay local by default while the baseline remains versioned. The JSON shape is planner discretion, but it must preserve metric comparability and explicit pass/fail fields per D-1010.</action>
  <verify>
    <automated>node scripts/perf/reduce-artifacts.mjs --baseline artifacts/perf/baseline.json --artillery artifacts/perf/artillery-current.json --latency artifacts/perf/latency-current.json --history-seed artifacts/perf/history-seed-current.json --playwright-critical artifacts/perf/playwright-critical-current.json --playwright-history artifacts/perf/playwright-history-current.json --current artifacts/perf/current.json --diff artifacts/perf/diff.json</automated>
  </verify>
  <done>The reducer creates normalized `current.json` and `diff.json` from raw gate outputs, comparing them against a committed `baseline.json` with explicit regression visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Build the strict release-gate entrypoint and script surface</name>
  <files>package.json, scripts/qa/release-gate.sh</files>
  <action>Create `scripts/qa/release-gate.sh` as the single blocking Phase 10 entrypoint. It must run, in order: `phase10-startup-gate.sh`, perf-lite smoke, p95 latency probe, 100k history seed, the critical Playwright UAT suite with machine-readable output at `artifacts/perf/playwright-critical-current.json` via `PLAYWRIGHT_JSON_OUTPUT_NAME=artifacts/perf/playwright-critical-current.json pnpm exec playwright test e2e/realtime/ws-auth.spec.ts e2e/realtime/room-realtime.spec.ts e2e/realtime/dm-realtime.spec.ts e2e/realtime/presence.spec.ts e2e/realtime/autoscroll.spec.ts e2e/realtime/reconnect.spec.ts e2e/realtime/unread-sidebar.spec.ts e2e/realtime/history-load.spec.ts --reporter=json`, the dedicated 100k proof with machine-readable output at `artifacts/perf/playwright-history-current.json` via `PLAYWRIGHT_JSON_OUTPUT_NAME=artifacts/perf/playwright-history-current.json pnpm exec playwright test e2e/perf/history-100k.spec.ts --reporter=json`, and then the artifact reducer with explicit input flags for the Artillery, latency, history-seed, critical-Playwright, and history-Playwright JSON files. Update `package.json` with discoverable commands for startup-only, perf-lite, latency probe, critical Playwright UAT, dedicated history proof, reducer, and the full release gate. Preserve Playwright as the sole browser automation path; do not add a second browser runner or a non-blocking shortcut around D-1007.</action>
  <verify>
    <automated>bash scripts/qa/release-gate.sh</automated>
  </verify>
  <done>One command runs every blocking Phase 10 gate in the required order, writes `artifacts/perf/playwright-critical-current.json` and `artifacts/perf/playwright-history-current.json`, passes those exact paths into the reducer, emits normalized artifacts, and exits non-zero on any regression or gate failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release orchestrator→all gate scripts | One shell entrypoint chains multiple verification tools and must preserve failure semantics |
| reducer→machine-readable artifacts | Metric normalization determines release outcomes and must not hide regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | `scripts/perf/reduce-artifacts.mjs` | mitigate | Compute explicit per-gate pass/fail and metric deltas from raw inputs; never accept missing inputs silently |
| T-10-11 | Repudiation | `scripts/qa/release-gate.sh` | mitigate | Enforce fixed gate ordering and write deterministic artifact file names for every run |
| T-10-12 | Elevation of privilege | browser automation policy | mitigate | Keep Playwright as the only browser runner and route all browser proof through the repo-standard config |
</threat_model>

<verification>
Run `bash scripts/qa/release-gate.sh`. Confirm it writes `artifacts/perf/playwright-critical-current.json`, `artifacts/perf/playwright-history-current.json`, `artifacts/perf/current.json`, and `artifacts/perf/diff.json` from raw gate outputs and exits non-zero when any startup, perf-lite, latency, critical Playwright UAT, or 100k-history gate fails.
</verification>

<success_criteria>
Phase 10 has a single strict release gate plus baseline/current/diff artifacts that make regressions explicit and block release on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-qa-and-release-hardening/10-04-SUMMARY.md`
</output>
