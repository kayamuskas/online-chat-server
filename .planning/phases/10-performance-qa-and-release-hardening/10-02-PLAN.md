---
phase: 10-performance-qa-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - package.json
  - pnpm-lock.yaml
  - scripts/perf/perf-lite.yml
  - scripts/perf/probe-latency.mjs
  - scripts/perf/perf-shared.mjs
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "Phase 10 has a blocking perf-lite gate instead of a vague 'performance looks okay' claim."
    - "Latency acceptance is based on p95 message delivery and p95 presence propagation per D-1003 and D-1004."
    - "Perf-lite measurements exercise the live HTTP plus Socket.IO product path, not synthetic console timing alone."
  artifacts:
    - path: "scripts/perf/perf-lite.yml"
      provides: "Artillery smoke scenario with threshold-enforced perf-lite checks"
    - path: "scripts/perf/probe-latency.mjs"
      provides: "Product-aware p95 latency probe for message-created and presence events"
    - path: "scripts/perf/perf-shared.mjs"
      provides: "Shared auth/setup/helpers for perf-lite actors and probe orchestration"
  key_links:
    - from: "scripts/perf/perf-lite.yml"
      to: "http://localhost:3000/api/v1"
      via: "HTTP and Socket.IO smoke traffic"
      pattern: "target: 'http://localhost:3000'"
    - from: "scripts/perf/probe-latency.mjs"
      to: "apps/api realtime event contract"
      via: "`message-created` and `presence` event observation"
      pattern: "message-created|presence"
    - from: "scripts/perf/probe-latency.mjs"
      to: "artifacts/perf/"
      via: "raw current-run JSON output for later reduction"
      pattern: "artifacts/perf"
---

<objective>
Implement the blocking perf-lite smoke and p95 latency evidence layer.

Purpose: satisfy D-1001, D-1003, and D-1004 without drifting into deferred
full-scale benchmarking. The gate must prove the live stack meets the v1
release bar using threshold-enforced smoke plus product-aware p95 probes.

Output: an Artillery smoke harness plus a Socket.IO-aware latency probe that
write raw current-run evidence for later release-gate reduction.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-performance-qa-and-release-hardening/10-CONTEXT.md
@.planning/phases/10-performance-qa-and-release-hardening/10-RESEARCH.md
@scripts/qa/phase10-startup-gate.sh
@package.json
@e2e/helpers/api-setup.ts

<interfaces>
From e2e/helpers/api-setup.ts:
```ts
export interface TestUser {
  id: string;
  email: string;
  username: string;
  password: string;
  cookieHeader: string;
}

export async function createAndSignIn(opts?: { suffix?: string }): Promise<TestUser>;
export async function createRoom(user: TestUser, name?: string): Promise<TestRoom>;
export async function joinRoom(user: TestUser, roomId: string): Promise<void>;
```

From package.json:
```json
{
  "scripts": {
    "test:e2e": "playwright test"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install root perf dependencies and add the perf-lite smoke harness per D-1001</name>
  <files>package.json, pnpm-lock.yaml, scripts/perf/perf-lite.yml, scripts/perf/perf-shared.mjs</files>
  <action>Update the root workspace manifest and lockfile to add both `artillery@2.0.30` and `socket.io-client@4.8.3` as Phase 10 devDependencies before any perf-lite or probe verification runs, resolving the repo-root tooling contract documented in `10-RESEARCH.md`. Then create the Artillery-backed perf-lite scenario that hits the live local stack after the startup gate passes. Use `perf-shared.mjs` for deterministic actor setup, cookie-authenticated room membership, and scenario variables so the YAML stays declarative. The scenario must stay intentionally smoke-scale per D-1001, but it must fail on threshold breaches and produce raw JSON output under `artifacts/perf/` for later reduction. Do not try to simulate the deferred 300-user full benchmark from D-1002.</action>
  <verify>
    <automated>rg -n '"artillery": "2.0.30"|"socket.io-client": "4.8.3"' package.json pnpm-lock.yaml && pnpm install --frozen-lockfile && bash scripts/qa/phase10-startup-gate.sh && pnpm exec artillery run scripts/perf/perf-lite.yml --output artifacts/perf/artillery-current.json</automated>
  </verify>
  <done>The repo root resolves both `pnpm exec artillery ...` and the repo-root `socket.io-client` import used by the latency probe, and the perf-lite harness runs against the live stack, exits non-zero on threshold failure, and emits raw Artillery JSON to `artifacts/perf/artillery-current.json`.</done>
</task>

<task type="auto">
  <name>Task 2: Measure blocking p95 business-event latency</name>
  <files>scripts/perf/probe-latency.mjs, scripts/perf/perf-shared.mjs</files>
  <action>Implement `probe-latency.mjs` to measure end-to-end business-event timing for both blocking latency gates in D-1003: room message delivery p95 &lt; 3s and presence propagation p95 &lt; 2s. Use the app's real auth and Socket.IO contract, with one actor triggering the event and another observing `message-created` or `presence`. Persist raw samples plus computed percentiles in JSON under `artifacts/perf/latency-current.json`. Do not report mean-only acceptance; p95 must be the explicit pass/fail metric per D-1004.</action>
  <verify>
    <automated>bash scripts/qa/phase10-startup-gate.sh && node scripts/perf/probe-latency.mjs --output artifacts/perf/latency-current.json</automated>
  </verify>
  <done>The latency probe writes raw samples and computed p95 values for message delivery and presence propagation, and the script exits non-zero when either D-1003 threshold is missed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| perf harness→live API/socket stack | Synthetic traffic hits authenticated application surfaces |
| perf setup helpers→session cookies | The harness handles authenticated cookies and must avoid leaking them into docs artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information disclosure | `scripts/perf/perf-shared.mjs` | mitigate | Keep session cookies in ephemeral runtime memory and exclude them from JSON artifacts |
| T-10-05 | Spoofing | perf actor setup | mitigate | Reuse the real register/sign-in flow from existing test helpers instead of bypass credentials |
| T-10-06 | Repudiation | latency evidence | mitigate | Persist raw samples plus computed percentiles so pass/fail is auditable |
</threat_model>

<verification>
1. `bash scripts/qa/phase10-startup-gate.sh`
2. `pnpm install --frozen-lockfile`
3. `pnpm exec artillery run scripts/perf/perf-lite.yml --output artifacts/perf/artillery-current.json`
4. `node scripts/perf/probe-latency.mjs --output artifacts/perf/latency-current.json`
</verification>

<success_criteria>
Phase 10 has a repeatable perf-lite gate and a product-aware p95 latency probe that enforce D-1001, D-1003, and D-1004 with raw machine-readable outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-qa-and-release-hardening/10-02-SUMMARY.md`
</output>
