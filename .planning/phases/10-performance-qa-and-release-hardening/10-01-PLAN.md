---
phase: 10-performance-qa-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/qa/phase10-startup-gate.sh
  - scripts/qa/phase10-gate-common.sh
autonomous: true
requirements:
  - PERF-01
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "Release hardening starts with a blocking fresh-clone startup gate, not an optional smoke check."
    - "Startup hardening reuses the existing Compose/offline contract instead of inventing a second bootstrap path."
    - "Failure in startup, offline dependency checks, transport, or queue smoke stops later Phase 10 gates."
  artifacts:
    - path: "scripts/qa/phase10-startup-gate.sh"
      provides: "Strict startup preflight for Phase 10 release hardening"
    - path: "scripts/qa/phase10-gate-common.sh"
      provides: "Shared shell helpers for deterministic gate execution and failure reporting"
  key_links:
    - from: "scripts/qa/phase10-startup-gate.sh"
      to: "scripts/qa/phase1-smoke.sh"
      via: "blocking startup invocation"
      pattern: "phase1-smoke\\.sh"
    - from: "scripts/qa/phase10-startup-gate.sh"
      to: "scripts/qa/phase1-offline-check.sh"
      via: "blocking offline/startup contract invocation"
      pattern: "phase1-offline-check\\.sh"
    - from: "scripts/qa/phase10-startup-gate.sh"
      to: "scripts/qa/phase1-queue-check.sh|scripts/qa/phase1-transport-check.sh"
      via: "release-gate prerequisite chaining"
      pattern: "phase1-(queue|transport)-check\\.sh"
---

<objective>
Create the release-startup preflight required by Phase 10.

Purpose: enforce D-1007 by making fresh-clone startup, offline dependency
integrity, transport reachability, and queue wiring release-blocking before any
performance or browser gates run.

Output: a deterministic Phase 10 startup gate script that reuses the proven
Phase 1 smoke scripts and exits non-zero on any preflight failure.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-performance-qa-and-release-hardening/10-CONTEXT.md
@.planning/phases/10-performance-qa-and-release-hardening/10-RESEARCH.md
@.planning/phases/01-foundation-and-offline-delivery/01-04-SUMMARY.md
@scripts/qa/phase1-smoke.sh
@scripts/qa/phase1-offline-check.sh
@scripts/qa/phase1-queue-check.sh
@scripts/qa/phase1-transport-check.sh
@infra/compose/compose.yaml

<interfaces>
From scripts/qa/phase1-smoke.sh:
```bash
docker compose -f "${COMPOSE_FILE}" config --quiet
docker compose -f "${COMPOSE_FILE}" up --build --wait --timeout 180
```

From scripts/qa/phase1-offline-check.sh:
```bash
if rg --quiet -n "${CDN_PATTERNS}" "${REPO_ROOT}/apps/web/index.html" "${REPO_ROOT}/apps/web/src/"; then
  check_fail "CDN/hosted asset references found in web source"
fi
```

From infra/compose/compose.yaml:
```yaml
api:
  healthcheck:
    test: ["CMD-SHELL", "curl -sf http://localhost:3000/healthz || exit 1"]
web:
  healthcheck:
    test: ["CMD-SHELL", "curl -sf http://localhost:4173/healthz || exit 1"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the strict startup preflight wrapper</name>
  <files>scripts/qa/phase10-startup-gate.sh, scripts/qa/phase10-gate-common.sh</files>
  <action>Create `phase10-startup-gate.sh` as the Phase 10 preflight entrypoint for D-1007. It must call the existing `phase1-smoke.sh`, `phase1-offline-check.sh`, `phase1-queue-check.sh`, and `phase1-transport-check.sh` in a fixed blocking order, with shared helper functions in `phase10-gate-common.sh` for step banners, timing, and immediate non-zero exit on failure. Do not duplicate the Phase 1 check logic; reuse the existing scripts so OPS-01/OPS-02 remain anchored to the same contract.</action>
  <verify>
    <automated>bash scripts/qa/phase10-startup-gate.sh</automated>
  </verify>
  <done>`phase10-startup-gate.sh` fails fast when any startup prerequisite fails and passes only when the existing compose/offline/queue/transport checks all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Make startup evidence readable and reusable by later gates</name>
  <files>scripts/qa/phase10-startup-gate.sh, scripts/qa/phase10-gate-common.sh</files>
  <action>Add structured step output and stable exit semantics so later Phase 10 scripts can invoke the startup gate without parsing ad hoc console text. Capture per-step elapsed seconds and print a compact PASS/FAIL summary suitable for release logs, but keep the artifact contract out of this plan; JSON artifact synthesis belongs to later Phase 10 work.</action>
  <verify>
    <automated>bash scripts/qa/phase10-startup-gate.sh >/tmp/phase10-startup.log && rg -n "PASS|FAIL|phase1-smoke|phase1-offline-check|phase1-queue-check|phase1-transport-check" /tmp/phase10-startup.log</automated>
  </verify>
  <done>The startup gate emits stable step names, elapsed timings, and an overall PASS/FAIL summary that later release orchestration can call directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local operator→docker compose | A human-triggered release gate starts containers and trusts healthchecks |
| shell wrapper→existing smoke scripts | The new wrapper depends on inherited script exit codes and must not mask failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `scripts/qa/phase10-startup-gate.sh` | mitigate | Call existing scripts directly with `set -euo pipefail`; no string-based success parsing |
| T-10-02 | Repudiation | startup evidence output | mitigate | Print stable step names and elapsed timing for each prerequisite execution |
| T-10-03 | Denial of service | compose startup gate | mitigate | Keep the gate ordered and fail fast before expensive perf/browser checks start |
</threat_model>

<verification>
Run `bash scripts/qa/phase10-startup-gate.sh` against the local Compose stack. The command must succeed only when the Phase 1 startup, offline, queue, and transport checks all pass.
</verification>

<success_criteria>
Phase 10 has a strict startup preflight aligned with D-1007, and later plans can depend on it as the only supported release-startup entrypoint.
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-qa-and-release-hardening/10-01-SUMMARY.md`
</output>
