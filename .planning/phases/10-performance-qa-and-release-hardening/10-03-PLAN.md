---
phase: 10-performance-qa-and-release-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - scripts/perf/seed-history.mjs
  - e2e/perf/history-100k.spec.ts
  - e2e/global-setup.ts
  - e2e/helpers/ui-helpers.ts
autonomous: true
requirements:
  - PERF-02
  - TEST-01
must_haves:
  truths:
    - "A 100,000+ message room can be seeded deterministically without browser-driven setup."
    - "Playwright is the sole browser automation framework used to prove progressive upward history loading per D-1005."
    - "The browser gate checks chronology, monotonic watermarks, and duplicate-free progressive loading for the 100k scenario per D-1006."
  artifacts:
    - path: "scripts/perf/seed-history.mjs"
      provides: "Deterministic large-history dataset seeding for perf rooms"
    - path: "e2e/perf/history-100k.spec.ts"
      provides: "Blocking Playwright proof for PERF-02"
    - path: "e2e/global-setup.ts"
      provides: "Shared auth-state and perf fixture bootstrap updates"
    - path: "e2e/helpers/ui-helpers.ts"
      provides: "Reusable browser helpers for upward-scroll assertions"
  key_links:
    - from: "scripts/perf/seed-history.mjs"
      to: "messages history tables/endpoints"
      via: "deterministic large-history fixture creation"
      pattern: "api/v1/messages|messages"
    - from: "e2e/perf/history-100k.spec.ts"
      to: "playwright.config.ts"
      via: "repo-standard Playwright runner"
      pattern: "playwright test"
    - from: "e2e/perf/history-100k.spec.ts"
      to: "scripts/perf/seed-history.mjs"
      via: "seeded dataset prerequisite"
      pattern: "100000|100k"
---

<objective>
Create the blocking 100k-history browser proof for PERF-02.

Purpose: satisfy D-1005 and D-1006 with automated Playwright evidence that the
existing upward-history UI still behaves correctly when the room contains a
very large history.

Output: a deterministic seed helper plus a Playwright spec that proves
progressive upward loading, chronology integrity, monotonic watermark behavior,
and duplicate-free windows for a 100,000+ message room.
</objective>

<execution_context>
@/Users/kayama/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kayama/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-performance-qa-and-release-hardening/10-CONTEXT.md
@.planning/phases/10-performance-qa-and-release-hardening/10-RESEARCH.md
@.planning/phases/09-frontend-productization/09-04-SUMMARY.md
@playwright.config.ts
@e2e/global-setup.ts
@e2e/helpers/ui-helpers.ts
@e2e/realtime/history-load.spec.ts

<interfaces>
From playwright.config.ts:
```ts
export default defineConfig({
  globalSetup: './e2e/global-setup.ts',
  testDir: './e2e',
  workers: 1,
  reporter: 'list',
});
```

From e2e/global-setup.ts:
```ts
export interface E2EFixtures {
  alice: { id: string; email: string; username: string; password: string; cookieHeader: string };
  bob: { id: string; email: string; username: string; password: string; cookieHeader: string };
  room: { id: string; name: string };
  dm: { id: string };
}
```

From e2e/realtime/history-load.spec.ts:
```ts
const timeline = pageBob.locator('.msg-timeline');
const firstItem = timeline.locator('li.msg-bubble').first();
const beforeFirstWatermark = Number(await firstItem.getAttribute('data-watermark'));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed a deterministic 100,000+ message history room</name>
  <files>scripts/perf/seed-history.mjs, e2e/global-setup.ts</files>
  <action>Create `seed-history.mjs` to provision a dedicated large-history room and insert a deterministic 100,000+ message dataset using the real local stack's data model and auth contracts. Update `e2e/global-setup.ts` only as needed to expose the seeded room metadata and auth state to Playwright without polluting the existing realtime suite. Keep setup browser-light: do not send 100,000 messages through the UI.</action>
  <verify>
    <automated>bash scripts/qa/phase10-startup-gate.sh && node scripts/perf/seed-history.mjs --count 100000 --output artifacts/perf/history-seed-current.json</automated>
  </verify>
  <done>The seed helper creates a dedicated room fixture with at least 100,000 messages and writes machine-readable seed metadata for the Playwright spec to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Prove progressive upward history integrity in Playwright</name>
  <files>e2e/perf/history-100k.spec.ts, e2e/helpers/ui-helpers.ts</files>
  <action>Create a new `e2e/perf/history-100k.spec.ts` using Playwright only per TEST-01 and D-1005. Starting from the seeded room, scroll/load upward repeatedly and assert all four required behaviors from D-1006: progressive older-page loading, stable chronology in the loaded DOM window, strictly monotonic `data-watermark` order, and no duplicate rendered messages after multiple upward loads. Extend `ui-helpers.ts` only with helpers that make these browser assertions deterministic; do not add a second browser framework or screenshot-only proof.</action>
  <verify>
    <automated>bash scripts/qa/phase10-startup-gate.sh && node scripts/perf/seed-history.mjs --count 100000 --output artifacts/perf/history-seed-current.json && pnpm exec playwright test e2e/perf/history-100k.spec.ts --reporter=json</automated>
  </verify>
  <done>The new Playwright spec passes against the seeded 100k room and fails if upward loading regresses, chronology breaks, watermarks stop being monotonic, or duplicate rows appear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed helper→database/application data model | Large synthetic history is injected and must still respect message invariants |
| Playwright browser→authenticated local shell | Browser proof uses real session state and must stay within TEST-01 policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | `scripts/perf/seed-history.mjs` | mitigate | Seed data through application-compatible message records and preserve monotonic watermark ordering |
| T-10-08 | Denial of service | Playwright 100k spec | mitigate | Keep dataset creation outside the browser and scope assertions to loaded windows rather than the entire history DOM |
| T-10-09 | Information disclosure | Playwright auth state | mitigate | Reuse ignored storageState files only; never write session-bearing data into release docs artifacts |
</threat_model>

<verification>
1. `bash scripts/qa/phase10-startup-gate.sh`
2. `node scripts/perf/seed-history.mjs --count 100000 --output artifacts/perf/history-seed-current.json`
3. `pnpm exec playwright test e2e/perf/history-100k.spec.ts --reporter=json`
</verification>

<success_criteria>
Phase 10 has an automated Playwright-only proof that a 100,000+ message room still supports progressive upward history loading with integrity checks, satisfying PERF-02 and TEST-01.
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-qa-and-release-hardening/10-03-SUMMARY.md`
</output>
